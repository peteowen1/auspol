% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/salience_screen.R
\name{salience_permit_for}
\alias{salience_permit_for}
\title{The screen's permit vector for one election, built once and shared}
\usage{
salience_permit_for(
  election,
  prev_election,
  region,
  returns = NULL,
  surge_threshold = 5
)
}
\arguments{
\item{election, prev_election}{Election labels as used in
\code{output/candidacies.csv} and \code{output/salience-v6.csv}.}

\item{region}{The region code (\code{"fed"}, \code{"vic"}, \code{"sa"}, \code{"nsw"}), for
\code{\link[=surging_parties]{surging_parties()}}.}

\item{returns}{Optional pre-computed \code{\link[=candidate_returns]{candidate_returns()}} result; computed
if \code{NULL}.}

\item{surge_threshold}{Passed to \code{\link[=surging_parties]{surging_parties()}}.}
}
\value{
A \code{data.table} of \code{seat}, \code{party}, \code{permit}, or \code{NULL} if
\code{output/salience-v6.csv} has no rows for \code{election}.
}
\description{
Wraps \code{\link[=candidate_returns]{candidate_returns()}}, \code{\link[=surging_parties]{surging_parties()}} and \code{\link[=salience_screen]{salience_screen()}}
into the single lookup every backtest harness needs: for each seat and party
class, may this candidate be treated as a potential emergence?
}
\details{
One implementation because five harnesses build shares five different ways,
and a governed-population definition assembled slightly differently in each
is how a bug like the Donato mismatch survives in one harness after being
fixed in another.
}
