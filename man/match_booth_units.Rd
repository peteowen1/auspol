% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/booth_projection.R
\name{match_booth_units}
\alias{match_booth_units}
\title{Match one election's booth units to a reference election's}
\usage{
match_booth_units(live, ref)
}
\arguments{
\item{live, ref}{data.tables with \verb{district, unit, booth_type} (any other
columns ignored). \code{live} is the election being counted, \code{ref} the one
the swing is measured against.}
}
\value{
data.table \verb{district, unit, booth_type, ref_district, ref_unit},
one row per matched live unit.
}
\description{
A unit is an ordinary voting centre (keyed by name) or a declaration vote
type (\code{early}, \code{postal}, \code{absent}, \code{provisional}, \code{marked_as_voted}).
Matched in-district by name first, then, for ordinary booths only, by a
name that is unique statewide in the reference (booths keep their names
across a redistribution). Everything else is unmatched and projected from
the seat's counted share at apply time.
}
