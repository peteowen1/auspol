% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/booth_projection.R
\name{project_seat_from_booths}
\alias{project_seat_from_booths}
\title{Project a seat's final first preferences from the booths counted so far}
\usage{
project_seat_from_booths(live, ref, counted_units)
}
\arguments{
\item{live}{data.table of ONE seat: \verb{unit, booth_type, cls, v} (votes so
far for counted units; the full result in a replay).}

\item{ref}{data.table of the same seat's matched reference rows, keyed to
the LIVE unit names: \verb{unit, booth_type, cls, v}.}

\item{counted_units}{character vector of live units already reported.}
}
\value{
data.table \verb{cls, projected, counted_share}, or \code{NULL} if no
counted unit has a reference (nothing to measure a swing on).
}
\description{
\verb{projected_final = counted votes + uncounted units projected from their reference result shifted by the seat's counted swing}, where the swing
for a class is the share-point change on matched counted units, pooled
by booth type when that type has counted units and over all types
otherwise. Uncounted units with no reference, and classes with no
reference presence in the seat, take the seat's counted share.
}
