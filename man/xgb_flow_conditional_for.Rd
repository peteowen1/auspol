% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgb_flow_override.R
\name{xgb_flow_conditional_for}
\alias{xgb_flow_conditional_for}
\title{Build an xgb-flows-v1 conditional flow list for one target election}
\usage{
xgb_flow_conditional_for(
  target_election,
  prev_election,
  region,
  min_events = 3L
)
}
\arguments{
\item{target_election, prev_election, region}{As elsewhere in this package.}

\item{min_events}{Only emit a key if the historical corpus has at least
this many events for it (matches \code{build_flow_matrix()}'s own \code{min_n}
default) -- below that, return nothing for the key and let the caller's
existing \code{pooled} fallback handle it, same failure-open behaviour as the
shipped mechanism.}
}
\value{
A named list, same shape as \code{build_flow_matrix()$conditional}, or
\code{NULL} if the model files are missing or nothing could be built.
}
\description{
Experimental, gated behind \code{AUSPOL_XGB_FLOWS} -- see
\code{scripts/fit_xgb_flows_v1.R} and \verb{docs/plans/prereg-xgb-flows-v1-2026-09-10.md}.
}
\details{
Predicts a replacement for \code{build_flow_matrix()}'s \code{conditional} list,
keyed identically (\code{"FROM|A+B+C"}), so it substitutes directly into an
existing \code{fm} object without touching \code{distribute_preferences()} or the
simulator. Enumerates every (from, survivor-set) key already observed
anywhere in the historical corpus (not per-seat -- \code{distribute_preferences()}
shares one conditional list across every seat in a simulation run, so a
genuinely per-seat prediction would need a core-engine change; this
substitutes a statewide-average version of the seat-level features
\code{fit_xgb_flows_v1.R} used, which is the honest simplification for this
first wired version, not a silent one).
}
