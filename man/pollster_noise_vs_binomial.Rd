% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scorecard.R
\name{pollster_noise_vs_binomial}
\alias{pollster_noise_vs_binomial}
\title{Each firm's implied poll-to-poll noise against the binomial floor}
\usage{
pollster_noise_vs_binomial(
  fits_by_cycle,
  factors,
  party = "ALP",
  n_ref = BINOMIAL_REF_N
)
}
\arguments{
\item{fits_by_cycle}{List of \code{\link[=fit_cycle_trends]{fit_cycle_trends()}} results.}

\item{factors}{From \code{\link[=estimate_firm_factors]{estimate_firm_factors()}}.}

\item{party}{Party to measure on.}

\item{n_ref}{Reference sample size for the binomial floor. Defaults to
\link{BINOMIAL_REF_N}, shared with the \code{H1}/\code{L4b} pipeline checks so the
published figure and the halting check describe the same quantity.}
}
\value{
data.table: \code{firm}, \code{implied_sd_pts}, \code{binomial_floor}, \code{ratio},
\code{n_polls}. A ratio below 1 is the herding signal.
}
\description{
The herding measure, and it must be ABSOLUTE. \code{\link[=estimate_firm_factors]{estimate_firm_factors()}}
returns a RELATIVE factor, normalised so the poll-weighted average is one;
a value of 0.7 means "quieter than the average Australian pollster", which
says nothing about sampling theory if the whole field is quiet. Reading
those factors as a herding check — as an earlier version of the scorecard
did — compares pollsters with each other and calls the answer physics.
}
\details{
The absolute figure is the fitted \code{sigma_obs} for the cycle, which is
estimated by marginal likelihood and so is not shrunk by the trend,
multiplied by the firm's factor and expressed in percentage points at the
level the party actually polled. Below the binomial sd for a poll of
\code{n_ref} respondents, a firm's polls agree with each other more closely than
random sampling permits.
}
