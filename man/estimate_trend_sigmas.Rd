% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyperpars.R
\name{estimate_trend_sigmas}
\alias{estimate_trend_sigmas}
\title{Estimate sigma_obs and sigma_rw for one party by marginal likelihood}
\usage{
estimate_trend_sigmas(
  polls_list,
  party,
  prior_results = NULL,
  sigma_house = 3,
  min_firm_polls = 3,
  firm_factors = NULL,
  min_polls = 25,
  start = c(1.7, 0.1),
  lower = c(0.5, 0.015),
  upper = c(4, 0.6)
)
}
\arguments{
\item{polls_list}{List of cycle polls (each from \code{\link[=cycle_polls]{cycle_polls()}}).}

\item{party}{Party column name.}

\item{prior_results}{Numeric vector of day-0 anchors, one per cycle
(NA = anchor loosely to first poll).}

\item{sigma_house, min_firm_polls, firm_factors}{As in \code{\link[=fit_trend]{fit_trend()}}.}

\item{min_polls}{Cycles where the party has fewer polls than this are
dropped (too little data to inform the sigmas).}

\item{start}{Starting values c(sigma_obs, sigma_rw); also the reference
point for \code{logml0}.}

\item{lower, upper}{Box bounds c(sigma_obs, sigma_rw), in points.}
}
\value{
List: \code{sigma_obs}, \code{sigma_rw}, \code{logml} (at optimum), \code{logml0}
(at the starting values, for a monotonicity sanity check), \code{n_cycles},
\code{n_polls}, \code{at_bound}, \code{convergence} (0 = optim converged).
}
\description{
The Gaussian-exact trend model has an exact evidence (see \code{\link[=fit_trend]{fit_trend()}}'s
internals), so the two variance hyperparameters can be estimated by
maximising log marginal likelihood — no cross-validation, no MCMC. When
several cycles are supplied the log evidences are summed, i.e. one
(sigma_obs, sigma_rw) pair is assumed to hold across cycles; estimate from
PAST (completed) cycles and apply to the live one.
}
\details{
Optimisation is L-BFGS-B on the log scale. Hitting a box bound is reported
via \code{at_bound} — treat that as "the method is wrong", not as an estimate.
}
