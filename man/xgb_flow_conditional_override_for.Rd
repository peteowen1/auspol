% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgb_flow_override.R
\name{xgb_flow_conditional_override_for}
\alias{xgb_flow_conditional_override_for}
\title{Build a PER-SEAT xgb-flows-v1 conditional override list}
\usage{
xgb_flow_conditional_override_for(
  shares,
  target_election,
  prev_election,
  region,
  min_events = 3L
)
}
\arguments{
\item{shares}{Seats x parties numeric matrix -- the SAME shares matrix
passed to \code{simulate_seat_contests()}, so each seat's own actual primary
shares become the \code{to_primary}/\code{from_primary} features, not a
statewide average.}

\item{target_election, prev_election, region}{As elsewhere in this package.}

\item{min_events}{Only emit a key if the historical corpus has at least
this many events for it (matches \code{build_flow_matrix()}'s own \code{min_n}
default) -- below that, return nothing for the key and let the caller's
existing \code{pooled} fallback handle it, same failure-open behaviour as the
shipped mechanism.}
}
\value{
A list, one entry per \code{rownames(shares)}, each \code{NULL} or a named
list of named numeric vectors in \code{build_flow_matrix()$conditional}'s
own shape -- pass directly as \code{simulate_seat_contests(..., conditional_override = this)}.
}
\description{
The real per-seat version of \code{\link[=xgb_flow_conditional_for]{xgb_flow_conditional_for()}} -- built after
review found the statewide-average version could never be a fair test of
a model whose own training used each seat's OWN primary shares as a
feature. Returns one conditional dict per seat, keyed to
\code{simulate_seat_contests()}'s new \code{conditional_override} parameter
(\code{R/seat_sim.R}), which consults each seat's own dict before falling back
to the shared \code{matrix$conditional} table -- see that parameter's own
docs for exactly where in the simulation this is consulted.
}
