% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/salience_surge.R
\name{blend_salience_shares}
\alias{blend_salience_shares}
\title{Apply the salience point estimate to a seat-by-class share matrix}
\usage{
blend_salience_shares(shares, hz, surge_mu, expected = FALSE)
}
\arguments{
\item{shares}{Numeric matrix, seats in rows (named) and classes in columns
(named), percentages. Rows are renormalised to 100 on the way out.}

\item{hz}{The list from \code{\link[=surge_hazard_for]{surge_hazard_for()}}, or \code{NULL} (returns \code{shares}
unchanged, which is the case before a target election's corpus exists).}

\item{surge_mu}{Mean vote of past winners, the value blended toward.}

\item{expected}{When \code{TRUE}, use \code{hz$seat_party_expected}'s band mean as a
FLOOR instead of the hazard blend (P5; refused 2026-09-07, off by
default, kept because the wave term would act through it).}
}
\value{
\code{shares}, with an attribute \code{cells} giving how many (seat, class)
cells were moved.
}
\description{
The blend \code{surge_blend_estimate()} performs, lifted out of the federal
harness so every caller runs the same code. Until 2026-09-07 this lived
inline in \code{scripts/backtest_candidate_fed.R} alone: the Victorian, NSW and
SA harnesses used the hazard for the DRAW only, and \code{fit_seats_full.R} --
the published forecast -- did not blend at all, so the salience point
estimate had never reached the published Victoria forecast.
}
