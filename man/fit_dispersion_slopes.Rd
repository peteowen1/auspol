% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{fit_dispersion_slopes}
\alias{fit_dispersion_slopes}
\title{Fit the "new"-candidate slope from dispersion, leave-one-election-out}
\usage{
fit_dispersion_slopes(
  target_election,
  corpus = NULL,
  pairs = NULL,
  min_pairs = 6L,
  level_now = NULL,
  classes = c("GRN", "ONP")
)
}
\arguments{
\item{target_election}{Election being scored; excluded from every fit.}

\item{corpus}{Optional pre-read candidacy table.}

\item{pairs}{Optional pair list; \code{\link[=all_election_pairs]{all_election_pairs()}} when \code{NULL}. Must
include the pair that PRODUCES \code{target_election}, so its own "before"
seat history and (when \code{level_now} is not supplied) its own actual
statewide result can be read.}

\item{min_pairs}{Minimum OTHER pairs of a class needed to fit its
correlation and sd-curve. Below it, that class keeps the shipped
constant.}

\item{level_now}{Optional named numeric vector (by class) of the
statewide share to predict \code{sd_after} at. \code{NULL} (the default, used by
every backtest harness) reads the target election's own ACTUAL result
from the corpus -- appropriate for backtesting a known outcome. A live
forecast (no actual result yet) passes the trend model's forecast level
here instead.}

\item{classes}{Which classes to fit. Default is GRN and ONP only --
real, persistent, single-brand parties, which is what the corr x
sd-ratio mechanism assumes. \code{IND} and \code{OTH_RIGHT} are NOT parties in
that sense: \code{IND} is by definition a different person every election
(\code{classify_party()}'s own docs: "Independents are their own class ...
not other"), and \code{OTH_RIGHT} is a residual bucket \code{classify_party()}
files a dozen-plus unrelated minor-right parties into (DLP, Liberal
Democrats, Palmer United/UAP, Rise Up Australia, Family First, Shooters
Fishers Farmers, Katter's, and more -- see \code{R/parties.R}). Fitting IND
this way on 2026-09-09 gave fed2013 a slope of 0.071, crushing all 74
of its "new" independent seats toward the state mean, and made fed2013
-- the case the whole arm was built around -- WORSE overall despite
OTH_RIGHT's own fitted slope being closer to the truth (see the result
section of \verb{docs/plans/prereg-dispersion-slope-2026-09-09.md}). Classes
left out of \code{classes} keep the shipped flat constant untouched -- the
structurally correct default for a bucket with no brand continuity.}
}
\value{
A list: \code{same} (unchanged \code{SHIP_SAME}), \code{new} (named vector, one
entry fitted or falling back per class), \code{n} (a table of how many other
pairs each class had to fit on, for coverage reporting).
}
\description{
The flat \code{new} constant in \code{\link[=conditional_slopes]{conditional_slopes()}} averages hundreds of
ordinary no-hoper candidacies with the handful of real emergence/collapse
events, and is provably wrong on the events that matter most: shipped
\code{OTH_RIGHT} \code{new} = 0.325 is 3.1x off fed2013's actual slope (Palmer
United's debut), shipped \code{ONP} \code{new} = 0.545 is 3.7x off sa2026's.
}
\details{
For a no-intercept regression, \code{slope = corr(dev_before, dev_after) * sd(dev_after) / sd(dev_before)} is an exact identity. Neither ingredient
is stable POOLED ACROSS CLASSES (correlation ranges 0.75-0.94 for GRN down
to near-zero for OTH; sd-at-a-given-level differs 2.5x between IND and ONP
at the same statewide share) -- but each is far more stable within one
class, fit leave-target-out over that class's other pairs. Verified
2026-09-09 against the three real ONP/OTH_RIGHT emergence and collapse
events in the corpus: predicted slopes within 15-42\% of the true fitted
slope, versus the flat constants' 1.2x-3.7x errors on the same three
cases (\verb{docs/plans/prereg-dispersion-slope-2026-09-09.md}).

Falls back to the shipped flat constant for a class when: fewer than
\code{min_pairs} other pairs of that class exist to fit corr/the sd-curve on,
the target's own "new"-tier seat history is too thin to measure a real
\code{sd_before} (< 5 seats, or sd < 0.3 -- indistinguishable from noise), or
any intermediate fit is non-finite. The predicted slope is clipped to
\verb{[0, 3]}; anything outside that range is not credible and falls back too.

Only the \code{new} (no candidate of this class stood here before, and not
salience-screen-permitted) tier moves. \code{same} is returned unchanged at the
shipped constants -- this arm does not touch the returning-candidate
slope, the sitting-member tier, or the screen's 1.0 override.
}
