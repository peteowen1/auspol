% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_returns.R
\name{route_departed_origin}
\alias{route_departed_origin}
\title{Move a departed defector's vote to their origin party in the prior matrix}
\usage{
route_departed_origin(mat, election_from, election_to, frac, corpus = NULL)
}
\arguments{
\item{mat}{Prior share matrix, seats by classes.}

\item{election_from, election_to}{Election labels.}

\item{frac}{Share of the leader's vote to move, from
\code{\link[=fit_departed_origin_return]{fit_departed_origin_return()}}. \code{NULL} returns \code{mat} untouched.}

\item{corpus}{Candidacy corpus; \code{output/candidacies.csv} when \code{NULL}.}
}
\value{
\code{mat} with attribute \code{departed_origin}: list(applied, skipped, cases).
}
\description{
For each \code{\link[=departed_defectors]{departed_defectors()}} case, \code{frac * lead_pcv} points move from
the departed class's column to the origin party's column, in the seat's
row of \code{mat} (rows seats, columns classes, in points). The class then
decays under the departed slope on what is left. Missing seat or column:
skipped and named in \code{attr(, "departed_origin")}.
}
