% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reentry_prior.R
\name{reentry_sd_matrix}
\alias{reentry_sd_matrix}
\title{Widen simulated variance for the flat-ratio re-entry path}
\usage{
reentry_sd_matrix(shares, reentry, level_sd, level_mult = NULL, k_sd = 0)
}
\arguments{
\item{shares}{The post-fill share matrix (seats x parties), same object
passed to \code{simulate_seat_contests()}.}

\item{reentry}{\code{apply_reentry_prior()}'s \code{"reentry"} attribute: a
\code{data.frame} of \code{seat}, \code{party}, \code{n}, \code{path}. Rows with \code{path != "ratio"}
or non-finite \code{n} are ignored.}

\item{level_sd}{\code{c(a, b)}, the same constants passed to
\code{simulate_seat_contests()}.}

\item{level_mult}{Optional named vector of per-class multipliers, the same
object \code{simulate_seat_contests()}'s \code{level_mult} argument takes (\code{NULL}
means every class multiplies by 1, matching that function's own default).}

\item{k_sd}{A single non-negative constant, percentage points. The extra
sd added in quadrature is \code{k_sd / sqrt(n)}, so it is largest for the
classes with the least evidence and shrinks toward zero as \code{n} grows.}
}
\value{
A matrix the same shape and dimnames as \code{shares}, \code{NA} everywhere
except the ratio-path re-entry cells, suitable as \code{sd_override}.
}
\description{
\verb{docs/plans/prereg-reentry-flatratio-variance-2026-09-08.md}. A class with
too few re-entry rows to fit (\code{reentry_fit()}'s \code{min_n}, default 40) falls
back to a flat ratio, and that estimate is estimated on almost nothing --
as little as 1-2 rows for Labor and the Coalition in this corpus. The POINT
estimate cannot be improved with this little data (tested and refused,
\code{docs/NEXT-STEPS.md} 2026-09-08); this widens the SIMULATED UNCERTAINTY
instead, only for cells that went down the ratio path (\code{path == "ratio"}
in \code{\link[=apply_reentry_prior]{apply_reentry_prior()}}'s \code{"reentry"} attribute) -- a GLM-fitted cell
already carries its own residual-implied uncertainty and is left alone.
}
\details{
\code{k_sd = 0} is a no-op: every value returned is \code{NA}, i.e. "no opinion,
keep \code{level_sd}" in the \code{sd_override} contract (\code{\link[=simulate_seat_contests]{simulate_seat_contests()}}),
the same convention \code{\link[=salience_sd_matrix]{salience_sd_matrix()}} uses. This is dry-run case 1 of
the pre-registration and is asserted by this package's tests, not just
claimed here.
}
