% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{fit_major_departed_slope}
\alias{fit_major_departed_slope}
\title{Fit the slope a major party keeps on its seat deviation when its sitting member departs}
\usage{
fit_major_departed_slope(
  target_election,
  corpus = NULL,
  pairs = NULL,
  min_n = 40L
)
}
\arguments{
\item{target_election}{Pair excluded from the fit.}

\item{corpus, pairs}{As in \code{\link[=fit_conditional_slopes]{fit_conditional_slopes()}}.}

\item{min_n}{Minimum departed rows per class.}
}
\value{
list(slope, n) for the departed tier, (slope_present, n_present)
for every other ALP/LNP cell, and \code{rows}, the fitted data.
}
\description{
Leave-target-out over every other pair. For ALP and LNP separately, rows
where the class held the seat at the prior election and its member did
not return under that label (\code{mp_departed} from \code{\link[=candidate_returns]{candidate_returns()}}),
regress \code{actual - level_now} on \code{prior - level_prev} through the origin,
exactly the fit \code{\link[=fit_conditional_slopes]{fit_conditional_slopes()}} does for the minor classes.
Fewer than \code{min_n} rows for a class leaves it at 1 (the current
behaviour). docs/plans/prereg-major-departed-slope-2026-09-18.md.
}
