% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reentry_prior.R
\name{apply_reentry_prior}
\alias{apply_reentry_prior}
\title{Apply the re-entry prior to a seat-by-class share matrix}
\usage{
apply_reentry_prior(mat, standing, fit, lean_dt, state_share, eps = 0.01)
}
\arguments{
\item{mat}{Numeric matrix, seats in rows (named), classes in columns.}

\item{standing}{\code{data.frame} with \code{seat} and \code{party} for classes contesting
the target election, or a character vector of \code{"seat|class"} keys.}

\item{fit}{From \code{\link[=reentry_fit]{reentry_fit()}}.}

\item{lean_dt}{From \code{\link[=seat_lean]{seat_lean()}} on the PREVIOUS election.}

\item{state_share}{Named numeric vector, projected statewide share per class.}

\item{eps}{A cell at or below this counts as no prior vote.}
}
\value{
\code{mat} with qualifying cells filled and attribute \code{"reentry"} naming
what was set — print it, because a rule that fills nothing is an arm that
looks like it ran and did not.
}
\description{
Fills cells that are zero because the class did not contest the seat last
time AND is contesting it now. Every other cell is untouched, so a run with
no qualifying cell is byte-identical.
}
\details{
Whether a class is standing comes from the nomination list, which closes
before polling day, and the harnesses already read exactly that to ZERO an
independent column where nobody nominated. This is the same fact in the
other direction: that rule removes a party that is not standing, this one
gives a base to a party that is.
}
