% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seats.R
\name{simulate_seats}
\alias{simulate_seats}
\title{Simulate a seat count from a projected two-party vote}
\usage{
simulate_seats(
  seats,
  tpp_mean,
  tpp_sd,
  prev_tpp,
  seat_sd,
  region_sd = 0,
  n_sims = 20000,
  seed = NULL
)
}
\arguments{
\item{seats}{From \code{\link[=load_seats]{load_seats()}}.}

\item{tpp_mean, tpp_sd}{Projected ALP two-party share and its sd.}

\item{prev_tpp}{The previous election's statewide ALP two-party share, from
which the swing is measured.}

\item{seat_sd}{Spread of seat-specific deviations — the WITHIN-region
figure from \code{\link[=seat_swing_spread]{seat_swing_spread()}} when \code{region_sd} is also supplied.}

\item{region_sd}{Spread of regional effects. Zero reproduces the earlier
independent-seats behaviour.
Non-classic seats are not simulated — the model holds no two-candidate
margin for a contest that is not Labor against a major — so they are assumed
held by their incumbent. \code{alp_total} adds the ones Labor already holds to
the simulated count, and \strong{that is the figure to publish}. \code{seats_won}
counts classic seats only and is kept for diagnostics.}

\item{n_sims}{Number of simulations.}

\item{seed}{Optional RNG seed.}
}
\value{
List: \code{seats_won} (ALP classic-seat wins per simulation),
\code{alp_total} (\code{seats_won} plus non-classic seats Labor holds — the
publishable total), \code{alp_nonclassic} (that constant), \code{by_seat} (win
probability per seat), \code{n_classic}, \code{n_nonclassic}.
}
\description{
Three sources of variation, in descending order of how much they move the
seat total: the statewide result itself, drawn from the projection's own
uncertainty and shifting every seat together; a regional effect shared by
the seats in each region; and a residual specific to each seat, which
decides the close ones.
}
\details{
Leaving out the regional layer does not change any single seat's
probability much, but it makes the SEAT COUNT look more certain than it is,
because independent deviations average out across 83 seats while correlated
ones do not.
}
