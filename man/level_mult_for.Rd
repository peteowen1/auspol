% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seat_sim.R
\name{level_mult_for}
\alias{level_mult_for}
\title{Per-class slope multipliers for \code{\link[=simulate_seat_contests]{simulate_seat_contests()}}}
\usage{
level_mult_for(parties, m_ind = 1, m_oth = 1)
}
\arguments{
\item{parties}{Character vector of the share columns, i.e. \code{colnames(shares)}.}

\item{m_ind}{Slope multiplier for \code{IND}.}

\item{m_oth}{Slope multiplier for every non-major that is not \code{IND} -- \code{GRN},
\code{ONP}, \code{OTH}, \code{OTH_RIGHT} and anything else \code{classify_party()} emits.}
}
\value{
A named numeric vector over \code{parties}, or \code{NULL} when both
multipliers are 1, so the no-op reaches \code{simulate_seat_contests()} as
\code{NULL} rather than as a vector of ones.
}
\description{
Builds the named vector \code{level_mult} expects, for exactly the parties a seat
file carries, from two numbers: one for independents and one for every other
non-major. Majors always take 1.
}
\details{
Exists so the class definition lives in ONE place. Six callers need it --
\code{fit_seats_full.R} and the five candidate backtest harnesses -- and
\code{CLAUDE.md} records that a parameter wired into four harnesses and missed in
the fifth produced numbers that were read as findings for four days. A
hand-written class list in six files is six chances to disagree.
}
