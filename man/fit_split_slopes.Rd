% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{fit_split_slopes}
\alias{fit_split_slopes}
\title{Fit the returning and departed deviation slopes, leave-one-election-out}
\usage{
fit_split_slopes(target_election, corpus = NULL, pairs = NULL, min_n = 200L)
}
\arguments{
\item{target_election}{The election being scored; excluded from the fit.}

\item{corpus}{Optional pre-read candidacy table.}

\item{pairs}{Optional list of \code{list(election=, prev=)}; \code{\link[=all_election_pairs]{all_election_pairs()}}
when \code{NULL}. Exposed for tests.}

\item{min_n}{Minimum fitting rows required; below this both slopes come
back \code{NULL} and a caller should fall back to \code{\link[=dev_slope]{dev_slope()}}.}
}
\value{
A list: \code{s_ret}, \code{s_dep} (or \code{NULL}), \code{se_ret}, \code{se_dep}, \code{n}.
}
\description{
Fits the two slopes \code{\link[=split_dev_slope]{split_dev_slope()}} consumes, over every election pair
the corpus holds EXCEPT the one being scored -- the same discipline
\code{\link[=fit_defector_discount]{fit_defector_discount()}} uses, so a target election never contributes to
the slopes used to predict it.
}
\details{
Fitted on NON-MAJOR classes only, because that is the population the arm
acts on and majors are dominated by the statewide swing rather than by who
is standing. Regression through the origin of the seat's deviation from the
statewide level on the two decomposed deviations, exactly the quantity
\code{\link[=dev_slope]{dev_slope()}} is defined in terms of.

Measured pooled over all 22 pairs (2026-09-09): \code{s_ret} \strong{0.899}
(se 0.013), \code{s_dep} \strong{0.575} (se 0.013), separated by 17.6 SE. The
returning figure closely reproduces the frozen 0.907 the repo already
carried; the departed figure is far ABOVE its frozen 0.326 counterpart --
a departed candidate's vote persists considerably more than that constant
assumed.
}
