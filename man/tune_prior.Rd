% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune.R
\name{tune_prior}
\alias{tune_prior}
\title{Choose one prior by leave-one-election-out error}
\usage{
tune_prior(
  param,
  grid,
  incumbent,
  material = 0.02,
  horizons = c(30, 90, 180, 365, 730)
)
}
\arguments{
\item{param}{Name of the \code{\link[=fit_trend]{fit_trend()}} argument to vary.}

\item{grid}{Values to try. Pre-register this, do not tune it.}

\item{incumbent}{The shipped value; wins ties and near-ties.}

\item{material}{MAE a challenger must beat the incumbent by to displace it.}

\item{horizons}{Passed to \code{\link[=build_projection_data]{build_projection_data()}}.}
}
\value{
List with \code{table} (grid and MAE), \code{chosen}, \code{gain}, \code{verdict}.
}
\description{
Refits the entire projection backtest once per grid value, so this is slow
by construction: about a minute per value.
}
\details{
The fundamentals are fitted ONCE and shared across arms. They use no polling
data and cannot depend on a poll-model prior, so refitting them per arm
would only let something stochastic vary between arms and be misattributed
to the prior under test — the confound that made six unrelated experiment
arms agree to within 0.2\% in a sister project.
}
