% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_returns.R
\name{protect_personal_vote_cells}
\alias{protect_personal_vote_cells}
\title{Keep the re-entry prior from overwriting a more-informed personal-vote floor}
\usage{
protect_personal_vote_cells(reentry_cells, own_prev)
}
\arguments{
\item{reentry_cells}{A \code{data.frame}/\code{data.table} with \code{seat} and \code{party}
columns -- the \code{"reentry"} attribute from \code{\link[=apply_reentry_prior]{apply_reentry_prior()}} (via
\code{\link[=reentry_apply_harness]{reentry_apply_harness()}}), before it is written into \code{shares}.}

\item{own_prev}{\code{\link[=personal_prior_vote]{personal_prior_vote()}}'s output, or \code{NULL}.}
}
\value{
\code{reentry_cells} with any row matching an \code{own_prev} (seat, party)
pair removed.
}
\description{
\verb{docs/plans/prereg-reentry-personal-vote-priority-2026-09-08.md}. Every
harness applies \code{apply_reentry_prior()}'s fill to \code{shares} "on the
post-swing projection" -- unconditionally, even for a cell
\code{\link[=personal_prior_vote]{personal_prior_vote()}} (with \code{major_discount} set) already informed with
an identity-matched floor. Gareth Ward's Kiama (LNP 53.6\% -> IND, real
result 38.8\%) is the case: the generic re-entry GLM predicts IND at 13.1\%,
discarding the defector's own 15.1\% floor -- close in magnitude here, but
the mechanism is the wrong one to have the last word on a cell it has no
identity-specific information about. Pilbara wa2001 (Larry Graham, ALP ->
IND) is the same shape.
}
\details{
This is a filter on the WRITE, not a change to either mechanism: both
keep computing what they already compute, and only the collision at
write time changes. A no-op whenever \code{own_prev} is \code{NULL} (i.e.
\code{AUSPOL_DEFECT_DISCOUNT} off) or carries no matching cell.
}
