% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external_flows.R
\name{pool_external_flows}
\alias{pool_external_flows}
\title{Pool another jurisdiction's transfers into a backtest, filtered by date}
\usage{
pool_external_flows(tx, before, source, not_after = NULL, quiet = FALSE)
}
\arguments{
\item{tx}{\code{data.table} of transfers for the election being predicted from.}

\item{before}{Date, or a string \code{as.Date()} accepts, of the election being
PREDICTED. Not the election \code{tx} came from.}

\item{source}{\code{"qld"} or \code{"wa"}.}

\item{not_after}{Optional extra ceiling on what may be admitted, applied on
top of \code{before} by taking the earlier of the two. Its purpose is refusal W1
of \code{docs/plans/prereg-wa-flows.md}: Western Australia's earliest election
predates every backtest election, so unlike Queensland there is no set of
elections that naturally admits nothing and can serve as a control. Setting
this to 1990 admits nothing, and the run must then reproduce the baseline
byte-for-byte -- which tests the filter rather than the data.}

\item{quiet}{Suppress the progress line. The line reports which elections
were admitted; \code{CLAUDE.md} records an experiment whose edit never ran and
whose byte-identical output read as "this input does not matter", so the
default is to print what was applied.}
}
\value{
\code{tx} with the admitted rows appended, or \code{tx} unchanged when no
source election precedes \code{before}.
}
\description{
Admits only source elections held STRICTLY BEFORE the election being
predicted, so a backtest can never use a result that did not yet exist.
}
