% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{fit_cycle_trends_guarded}
\alias{fit_cycle_trends_guarded}
\title{Fit a cycle, forcing any structurally invalid fit onto the logit scale}
\usage{
fit_cycle_trends_guarded(
  polls,
  parties = NULL,
  priors = NULL,
  overrides = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{polls}{From \code{\link[=cycle_polls]{cycle_polls()}}.}

\item{parties}{Party columns to fit; default: all with >= 30 polls in cycle.}

\item{priors}{Named vector of previous-election results (percent).}

\item{overrides}{Named list of per-party argument lists passed to
\code{\link[=fit_trend]{fit_trend()}}, e.g. \code{list(ONP = list(sigma_rw = 0.25))} to let a volatile
populist party's latent walk move faster.}

\item{verbose}{Report escalations (default TRUE) — a silent scale switch
would hide a real modelling problem.}

\item{...}{Passed to \code{\link[=fit_trend]{fit_trend()}} for every party.}
}
\value{
As \code{\link[=fit_cycle_trends]{fit_cycle_trends()}}, with an \code{escalated} attribute naming any
parties moved to logit.
}
\description{
Wraps \code{\link[=fit_cycle_trends]{fit_cycle_trends()}} with the \code{\link[=scale_breaches]{scale_breaches()}} guard. Evidence
chooses the scale per party, but a fit whose 95\% band includes a negative
vote share is invalid regardless of its likelihood, so it is refitted on the
logit scale, which cannot leave (0, 100).
}
\details{
Escalation DROPS that party's estimated sigmas as well as its scale: they
were estimated in points and are meaningless as log-odds (a \code{sigma_obs} of
1.9 points is a plausible poll noise; 1.9 log-odds is not), so the refit
falls back to \code{\link[=default_sigmas]{default_sigmas()}} for the new scale.
}
