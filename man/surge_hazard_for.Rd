% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/salience_surge.R
\name{surge_hazard_for}
\alias{surge_hazard_for}
\title{Per-seat surge hazard for one target election, fit on every OTHER election}
\usage{
surge_hazard_for(
  target_election,
  target_prev,
  target_region,
  train_pairs,
  lambda_grid = c(0.5, 1, 2, 5, 10, 20, 50)
)
}
\arguments{
\item{target_election, target_prev, target_region}{The election to score.}

\item{train_pairs}{Other elections to fit on -- must NOT include the
target, or the target leaks into its own training data.}

\item{lambda_grid}{Candidate L2 penalties.}
}
\value{
A list: \code{seat_hazard} (\code{data.table} of \code{seat}, \code{surge_h}, party
collapsed -- for the stochastic hazard in \code{\link[=simulate_seat_contests]{simulate_seat_contests()}}),
\code{seat_party_hazard} (\code{data.table} of \code{seat}, \code{party}, \code{p_hat} -- for
\code{\link[=surge_blend_estimate]{surge_blend_estimate()}}'s point-estimate shift, which needs to land on
the right column), \code{surge_mu}, \code{surge_sd} (pooled from training-pair
governed winners' actual \code{pcv}), \code{lambda}, \code{n_train_winners}. \code{NULL} if
there is no training population to fit on.
}
\description{
Extends the existing \code{surge_h} mechanism in \code{\link[=simulate_seat_contests]{simulate_seat_contests()}} --
already fat-tail, not a point-estimate shift -- with a properly featured,
governed-population-gated model instead of the single-feature, ungated,
stale-instrument fit in \code{scripts/fit_salience_hazard.R}. See
\code{docs/plans/prereg-salience-surge-v2.md} for why this form and not a direct
vote-share regression (REFUSED 2026-08-26,
\verb{docs/reviews/salience-regression-refused-2026-08-26.md} -- a linear jump
term applied to every non-major candidate cannot tell "loud because
emerging" from "loud because famous incumbent", and gating to the governed
population excludes that failure mode by construction).
}
\details{
The lambda (L2 penalty) is picked by nested leave-one-election-out \strong{within
the training pairs only} -- the target election never informs its own
penalty, let alone its own fit.
}
