% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyperpars.R
\name{estimate_firm_factors}
\alias{estimate_firm_factors}
\title{Per-pollster noise factors from pooled standardised residuals}
\usage{
estimate_firm_factors(fits, k0 = 12, clip = c(0.6, 2))
}
\arguments{
\item{fits}{List of \code{\link[=fit_trend]{fit_trend()}} results (nest freely; flattened on
\code{residuals} presence).}

\item{k0}{Shrinkage prior weight in pseudo-polls.}

\item{clip}{Factors are clipped to this range.}
}
\value{
data.table: \code{firm}, \code{n} (residuals pooled), \code{raw_ratio}
(normalised variance ratio), \code{factor} (sd multiplier, shrunk + clipped).
Pass \code{setNames(out$factor, out$firm)} as \code{firm_factors} to \code{\link[=fit_trend]{fit_trend()}}.
}
\description{
Empirical-Bayes second stage: pool each firm's poll residuals across the
supplied fits (all parties, all cycles — noisiness is a property of the
firm's methodology, not of one party's series), standardise by each fit's
sigma_obs, normalise so the poll-weighted average squared residual is 1
(the fitted trend absorbs some noise, so raw standardised residuals sit
below 1), then shrink each firm's variance ratio toward 1 with prior
weight \code{k0} pseudo-polls. Approximate — the exact route is per-firm sigmas
inside the marginal likelihood — but cheap and stable.
}
