% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{split_slope_context}
\alias{split_slope_context}
\title{Everything a harness needs to use the split slope, or NULL when it is off}
\usage{
split_slope_context(election_from, election_to, corpus = NULL, enabled = NULL)
}
\arguments{
\item{election_from, election_to}{Election labels for the pair being run.}

\item{corpus}{Optional pre-read candidacy table.}

\item{enabled}{Logical; defaults to the \code{AUSPOL_SPLIT_SLOPE} environment
variable being \code{"1"}. Off by default, so an unset environment reproduces
the shipped model byte-for-byte.}
}
\value{
\code{NULL}, or a list with \code{s_ret}, \code{s_dep}, \code{n}, and \code{frac(party, seats)} returning the returning-vote fraction aligned to \code{seats}.
}
\description{
ONE constructor because six harnesses and the published forecast each build
their shares slightly differently, and a parameter wired into some but not
all of them produces numbers that look like findings -- the failure this
repo has already paid for twice (see \code{\link[=dev_slope]{dev_slope()}}'s own note, and the
frozen-0.282 defector discount \code{\link[=fit_defector_discount]{fit_defector_discount()}} replaced).
}
\details{
Returns \code{NULL} -- meaning "carry on with \code{\link[=dev_slope]{dev_slope()}} exactly as before" --
whenever the arm is off, the corpus is unavailable, or the fit does not
converge on enough rows. A caller therefore needs no branch beyond
\code{is.null()}.
}
