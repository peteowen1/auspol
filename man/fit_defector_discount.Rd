% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_returns.R
\name{fit_defector_discount}
\alias{fit_defector_discount}
\title{Fit the major-party defector's retention rate, pooled across every
jurisdiction, leave-one-target-out}
\usage{
fit_defector_discount(
  target_election,
  corpus = NULL,
  min_n = 5L,
  pairs = NULL,
  pooled = NULL,
  min_prior = 10
)
}
\arguments{
\item{target_election}{The election being scored. Its own (seat, party)
defector cases are excluded from the fit -- this is what keeps the fit
leave-one-out rather than fitting the number on the data it will be
scored against.}

\item{corpus}{Optional pre-read candidacy table; read from
\code{output/candidacies.csv} when \code{NULL}.}

\item{min_n}{Minimum defector cases required to return a fit. Below this,
\code{discount} is \code{NULL} -- the same "not enough data" guard the original
federal-only fit used (\verb{BF0d!}).}

\item{pairs}{Optional list of \code{list(election=, prev=)}, as returned by
\code{\link[=all_election_pairs]{all_election_pairs()}}; that function's real 22-pair list is used when
\code{NULL}. Exposed so tests can inject a small synthetic pair list instead
of depending on the real corpus.}

\item{pooled}{Include major-party candidates who were NOT the sitting
member. \code{NULL} reads \code{AUSPOL_DEFECT_POOLED} (\code{"1"} or \code{"2"} enable it).
\code{FALSE} fits on sitting members alone, the pre-2026-09-09 behaviour.}

\item{min_prior}{Minimum prior vote, in points, for a case to enter the
fit. Stops a retention RATIO being taken on a denominator too small to
mean anything -- Preece (Schubert sa2026) went 2.1\% to 21.7\%, a ratio of
10.14 that on its own made the losing group look unusable.}
}
\value{
A list: \code{discount} (median retention ratio, or \code{NULL}),
\code{discount_mp} and \code{discount_loser} (the same for prior sitting members
and everyone else, for the two-rate arm), \code{n} (cases used), \code{cases} (the
underlying data.table, for inspection).
}
\description{
\code{personal_prior_vote(major_discount = ...)} needs one number: what fraction
of a sitting major-party MP's vote survives when they defect and re-contest
their own seat under a non-major label. Before this function, that number
was fitted ONCE, inline, in \code{backtest_candidate_fed.R} alone (federal pairs
only, leave-one-election-out) and the other five harnesses hardcoded
\code{0.282} -- a frozen snapshot of one federal run, never refreshed and never
pooling the non-federal cases sitting in the same corpus (Ward/Kiama,
Graham/Pilbara, Northe/Morwell, Masters/Vasse and others).
\verb{docs/plans/harness-unification-2026-09-08.md} names this exact drift.
}
\details{
This pools every jurisdiction via \code{\link[=all_election_pairs]{all_election_pairs()}} (the shared list
built for exactly this leave-one-out pattern) instead of federal's own
narrower same-region chain, and every caller now shares one fit instead of
five carrying a stale copy and one recomputing it. Reported over all 22
pairs pooled: 14 defector cases, median retention 0.302 (individual ratios
0.13 to 0.86) -- close to, but not identical to, the federal-only 0.282
this replaces, because the federal-only fit is a narrower sample of the
same underlying population, not a different one.

Median, not mean: with 14 points spanning 0.13-0.86, the mean (0.378) is
pulled up by the two long-tenure independents who kept their seats (Ward
0.72, Graham 0.86) exactly as much as it should be, but a caller fitting a
FLOOR for the typical case is better served by the median, which is also
what the pre-existing federal fit already used.
}
