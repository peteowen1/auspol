% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/names.R
\name{match_key}
\alias{match_key}
\title{Match key for one candidate, at a chosen strictness}
\usage{
match_key(sur, giv, rule = c("initial", "surname", "full"))
}
\arguments{
\item{sur}{Character vector of surnames, from \code{\link[=surname_of]{surname_of()}}.}

\item{giv}{Character vector of first given names, from \code{\link[=given_of]{given_of()}}.}

\item{rule}{One of \code{"surname"}, \code{"initial"}, \code{"full"}.}
}
\value{
Character vector of match keys.
}
\description{
Which rule is right is an empirical question, so all three are available and
\code{scripts/estimate_candidate_persistence.R} reports the answer under each.
}
\details{
\itemize{
\item \code{"surname"} — surname alone. Joins two different people who share a surname
in one seat, which inflates the "same person" group.
\item \code{"initial"} — surname plus first initial. The electoral-research default.
It survives Kate/Katherine and Mike/Michael, but NOT the nicknames that
change the initial — Bob/Robert, Bill/William, Dick/Richard — all common in
Australian politics. Those split into two people under this rule.
\item \code{"full"} — surname plus whole first name. Strictest, and splits every
nickname case including Kate/Katherine.
}

No rule is safe in both directions. Surname-only wrongly JOINS two people
sharing a surname; the other two wrongly SPLIT one person recorded under two
first names. Splitting is the more dangerous error here, because it turns a
returning member into a fabricated emergence.
}
