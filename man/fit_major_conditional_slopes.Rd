% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{fit_major_conditional_slopes}
\alias{fit_major_conditional_slopes}
\title{Fit same/new conditional slopes for MAJOR parties, leave-target-out}
\usage{
fit_major_conditional_slopes(
  target_election,
  corpus = NULL,
  pairs = NULL,
  min_n = 40L
)
}
\arguments{
\item{target_election}{Election being scored; excluded from the fit.}

\item{corpus}{Optional pre-read candidacy table.}

\item{pairs}{Optional pair list; \code{\link[=all_election_pairs]{all_election_pairs()}} when \code{NULL}.}

\item{min_n}{Minimum observations for a class/tier cell to be fitted at
all. Below it the shipped constant is kept -- fitting a slope on a
handful of seats is how this repo has produced confident wrong numbers
before. ONP "same" sits just above this floor at 51.}
}
\description{
\code{\link[=conditional_slopes]{conditional_slopes()}}/\code{\link[=screened_slopes]{screened_slopes()}} have never had a same/new
distinction for ALP/LNP/NAT -- \code{dev_slope()}'s \code{default = 1} (full,
unconditional carryforward of a seat's premium over the state average)
applies to every major-party seat regardless of whether the sitting
member is standing again. Found 2026-09-16 tracing why Parramatta's
\code{base_pred} gave LNP's 2023 candidate the FULL +12.4 of Geoff Lee's 2019
personal-vote premium, undiscounted, after Lee retired --
\verb{docs/reviews/base-pred-blind-to-tonights-fixes-2026-09-16.md}.
}
\details{
Identical method to \code{\link[=fit_conditional_slopes]{fit_conditional_slopes()}} (\code{yy ~ 0 + dev}, no
intercept, split by whether the class had a returning candidate in that
seat, leave-target-out): reused rather than duplicated logic diverging.
The only real difference is the fallback -- there is no shipped
major-party same/new constant to fall back to (majors were never
conditioned at all), so a cell that fails \code{min_n} falls back to \code{1.0},
i.e. today's actual unconditioned behaviour, not a guessed number.
}
