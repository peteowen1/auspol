% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection.R
\name{projection_loo}
\alias{projection_loo}
\title{Honest out-of-sample evaluation of the whole projection}
\usage{
projection_loo(dat, w_grid = seq(0, 1, by = 0.01), debias = TRUE)
}
\arguments{
\item{dat}{From \code{\link[=build_projection_data]{build_projection_data()}}, with \code{fund_tpp}.}

\item{w_grid}{Candidate mix weights.}

\item{debias}{Whether to apply the held-out bias correction. Set FALSE to
test whether correcting for bias helps at all.}
}
\value{
data.table, one row per (election, horizon): \code{pred}, \code{err},
\code{z} (error over the held-out sd), plus the fitted \code{w}, \code{bias}, \code{sd_err}.
}
\description{
Everything the projection needs — the mix weight, the bias correction and
the error spread — is estimated from past elections, so scoring it on those
same elections flatters it. This refits all three with each election held
out and scores the prediction that results.
}
\details{
It answers the question the headline seat probability actually depends on:
are the intervals honest? A 95\% band that contains the truth 70\% of the
time makes every downstream probability wrong.
}
