% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_polls.R
\name{load_polls}
\alias{load_polls}
\title{Load voting-intention polls for a region}
\usage{
load_polls(region = "fed")
}
\arguments{
\item{region}{One of "fed", "nsw", "vic", "qld", "wa", "sa".}
}
\value{
data.table with columns \code{date}, \code{firm}, \code{tpp_published}, one
numeric column per party, and — only when the source file carries them —
\code{gl_approve} and \code{gl_disapprove}. Sets attributes \code{parties} AND \code{region};
the latter is relied on by \code{\link[=cycle_polls]{cycle_polls()}} and \code{\link[=unfold_others]{unfold_others()}}.
}
\description{
Reads the anchor project's hand-maintained poll file
(\verb{poll-data-<region>.csv}) into a tidy data.table. Party first-preference
columns like "ALP FP" become \code{ALP}; \verb{#N/A} becomes \code{NA}. The published TPP
(\verb{@TPP}, ALP share) is kept for display/validation only — forecasts derive
TPP from first preferences, matching the anchor methodology.
}
