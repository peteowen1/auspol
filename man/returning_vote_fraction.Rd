% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{returning_vote_fraction}
\alias{returning_vote_fraction}
\title{What fraction of a class's prior seat vote belongs to candidates standing again}
\usage{
returning_vote_fraction(election_from, election_to, corpus = NULL)
}
\arguments{
\item{election_from, election_to}{Election labels as in the corpus.}

\item{corpus}{Optional pre-read candidacy table; read from
\code{output/candidacies.csv} when \code{NULL}.}
}
\value{
A \code{data.table} of \code{seat}, \code{party}, \code{ret_frac} (in \verb{[0, 1]}),
\code{n_prior}, \code{n_returning}, keyed on the PRIOR election's seat naming
normalised via \code{\link[=normalise_seat]{normalise_seat()}} and reported under the target
election's own seat names where they match.
}
\description{
The seat model projects a class's seat share forward with ONE deviation
slope chosen by a BINARY flag -- did the class's leading candidate return
(\code{\link[=candidate_returns]{candidate_returns()}}'s \code{same}). That makes the projection depend on an
accident of who polls highest, and it is internally inconsistent: when the
leader is new the model keeps the whole class total, and when the leader is
a returner it keeps only that one person's share.
}
\details{
This is the input to the fix. For every (seat, class) at the target
election it reports what share of the PRIOR election's vote for that class
sat with candidates who are standing again, so the projection can give the
returning and departed portions their own slopes
(\code{\link[=split_dev_slope]{split_dev_slope()}}, \code{\link[=fit_split_slopes]{fit_split_slopes()}}).

Identity matching is \code{\link[=candidate_returns]{candidate_returns()}}'s: surname plus first initial,
matched across the SEAT rather than within the party label (a candidate who
changes party is the same person), and against both spellings of a renamed
seat. It inherits that matching's known weakness on common surnames.
}
