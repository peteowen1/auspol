% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/election_dates.R
\name{election_dates}
\alias{election_dates}
\title{Polling day for every election the corpus knows about}
\usage{
election_dates(labels = NULL)
}
\arguments{
\item{labels}{Optional character vector of election labels (\code{"fed2022"});
\code{NULL} returns the whole table.}
}
\value{
Named \code{Date} vector, names are election labels. Unknown labels
are an error, not \code{NA} -- a silently missing date would let a model
train on the future.
}
\description{
ONE table, in the package. Before 2026-09-18 each backtest harness carried
its own copy (\code{FED_DATE}, \code{WA_DATE}, \code{VIC_DATE}, \code{V2_DATES}, the Queensland
and SA \code{asof}/\code{flow_before} literals) -- the same six-copies-drift shape
\code{all_election_pairs()} exists to prevent for the pair list, and nothing
outside a harness could ask "what date was fed2022" without grepping one.
The point-in-time XGBoost training (\code{scripts/fit_xgb_primary_asat.R}) needs
exactly that question answered for every target at once: a model "as at"
election T may train only on pairs whose target polling day is strictly
before T's.
}
