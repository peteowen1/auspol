% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold.R
\name{folded_rows}
\alias{folded_rows}
\title{Polls where a modelled party is missing but its votes are inside "Others"}
\usage{
folded_rows(polls, party, sum_range = c(97, 103), oth = "OTH")
}
\arguments{
\item{polls}{A cycle's polls from \code{\link[=cycle_polls]{cycle_polls()}}.}

\item{party}{The modelled party that may be folded away.}

\item{sum_range}{Reported-FP total that counts as "already complete".}

\item{oth}{Name of the residual column.}
}
\value{
Logical vector, one per poll row.
}
\description{
Identified arithmetically rather than assumed: a poll whose reported first
preferences already sum to ~100 without party \code{p} must be carrying \code{p}
inside one of its reported categories. A poll that simply omits \code{p} without
absorbing it sums to roughly \code{100 - p} instead and is left alone.
}
