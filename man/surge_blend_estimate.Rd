% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/salience_surge.R
\name{surge_blend_estimate}
\alias{surge_blend_estimate}
\title{Blend a uniform-swing point estimate toward the surge magnitude}
\usage{
surge_blend_estimate(uniform_share, p_hat, surge_mu)
}
\arguments{
\item{uniform_share}{Numeric vector: the pre-blend point estimate.}

\item{p_hat}{Numeric vector, same length: this seat/party's fitted hazard.}

\item{surge_mu}{Target magnitude to blend toward.}
}
\value{
Numeric vector, same length as \code{uniform_share}.
}
\description{
\code{surge_hazard_for()} widens the SIMULATED tail via \code{surge_h}, but the
deterministic point estimate that feeds it (\code{dev_slope()}'s output) is
otherwise blind to salience entirely -- correlation(jump, projected share)
measured at 0.050 on fed2022, essentially zero. Governed winners with real
salience were projected at 7-37\% of their actual result (Zoe Daniel: 3.2\%
projected vs 34.5\% actual). This closes that gap directly: a probability-
weighted blend between the uniform-swing estimate and the fitted surge
size, using the SAME \code{p_hat} already fitted for the hazard -- no second
model, no new failure surface. At \code{p_hat} near 0 (the overwhelming
majority of governed candidates -- no-hopers), the estimate is
unchanged. At \code{p_hat} near 1, it moves toward \code{surge_mu}.
}
