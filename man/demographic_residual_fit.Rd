% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographic_residual.R
\name{demographic_residual_fit}
\alias{demographic_residual_fit}
\title{Correct a primary prediction toward what DEMOGRAPHICS explain about its error}
\usage{
demographic_residual_fit(
  cls,
  exclude_pair,
  features = DEMO_FEATURES,
  alphas = c(0, 0.25, 0.5, 0.75, 1),
  oof = "output/xgb-primary-v6-oof-predictions.csv",
  census = "output/census-features.csv",
  shuffle = 0L
)
}
\arguments{
\item{cls}{Party class.}

\item{exclude_pair}{The pair being predicted; excluded from the fit entirely.}

\item{features}{Census columns to offer the model.}

\item{alphas}{Elastic-net mixing values to search. 0 is ridge, 1 is lasso.}

\item{oof, census}{Source tables.}

\item{shuffle}{Control seed; see \code{\link[=.er_shuffle]{.er_shuffle()}}. 0 is the real fit.}
}
\value{
A list with \code{b} (named coefficient vector), \code{alpha}, \code{lambda} and
\code{n} (training rows), or \code{NULL} when the class cannot be fitted.
}
\description{
Arm A of \verb{docs/plans/prereg-demographic-axis-2026-09-15.md}. The
single-feature ancestor in \code{\link[=education_residual_apply]{education_residual_apply()}} was REFUSED: it bet
everything on \code{yr12_pct} and its \code{born_aus_pct} placebo was not a placebo at
all, correlating -0.706 with it over 1,989 seats.
}
\details{
This fits ALL seven census columns at once under an elastic net, so each
column earns its own penalised coefficient instead of one being hand-picked,
and the honest control is the within-pair permutation in \code{\link[=.er_shuffle]{.er_shuffle()}}
rather than a swapped column.

THREE PROPERTIES THAT ARE DESIGN, NOT ACCIDENT:
\itemize{
\item \strong{Within-pair z.} Mean Year 12 completion drifts 51.4 to 60.8 across the
corpus, so a raw value carries that drift between elections and a tree or a
linear fit reads it as a jurisdiction label. This is the fault
\verb{reviews/xgb-primary-sd-and-census-2026-09-12.md} found when census was
first tried as a model feature.
\item \strong{No intercept.} Each \code{z} is mean-zero within its pair, so a coefficient
vector with no intercept produces corrections that also sum to zero across
a pair's seats. The correction therefore MOVES vote between seats and
leaves the statewide total for each class alone. An intercept would shift
every seat together and silently re-level the state.
\item \strong{Folds are PAIRS.} \code{lambda} and \code{alpha} are chosen by leave-one-training-
pair-out CV, never by row-wise folds. Seats within an election share
campaign, leader and statewide swing, so row-wise folds would leak a pair
into its own validation set and choose a penalty far too weak.
}

LEAKAGE: the model for pair X is fitted on every pair EXCEPT X, from
out-of-fold predictions, and the penalty is tuned inside that training set.
Nothing from the election being predicted reaches its own coefficients.
}
