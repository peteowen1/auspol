% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_polls.R
\name{flows_for}
\alias{flows_for}
\title{Preference flows for a cycle, carrying forward when a year is missing}
\usage{
flows_for(
  flows,
  year,
  region,
  quiet = FALSE,
  estimate = TRUE,
  cycles = NULL,
  as_of = Sys.Date()
)
}
\arguments{
\item{flows}{From \code{\link[=load_preference_flows]{load_preference_flows()}}.}

\item{year, region}{The cycle wanted.}

\item{quiet}{Suppress the carry-forward message.}

\item{estimate}{Replace assumed flows with estimates from the observed
record (\code{\link[=estimate_flows_for]{estimate_flows_for()}}). A no-op for an election already held,
where the file records what happened; for one still ahead, every value is
an assumption and gets estimated. Defaults to TRUE because a hand-set or
carried-forward number cannot respond to a new election, and this project
estimates its assumptions rather than freezing them.}

\item{cycles}{From \code{\link[=load_election_cycles]{load_election_cycles()}}; loaded if missing.}

\item{as_of}{Date defining which elections count as already held.}
}
\value{
data.table of the same shape, with a \code{flow_year} column recording
which election each estimate actually came from.
}
\description{
The anchor's flow file is hand-maintained and incomplete: Victoria, for
instance, has estimates only for 2018, nothing for 2022 or 2026. Selecting
on year alone returns zero rows, and \code{\link[=derive_tpp]{derive_tpp()}} then falls back to a
50-50 split for every party — badly wrong for the Greens, who send about
82\% of their preferences to Labor. A silent 50-50 would move Victorian TPP
by several points with nothing failing.
}
\details{
This carries each party's most recent estimate for that region forward to
the requested year, and says so. It never reaches across regions:
preference behaviour differs by state, and optional preferential voting
makes exhaust rates region-specific.
}
