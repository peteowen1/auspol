% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_mode.R
\name{statewide_draws_as_at}
\alias{statewide_draws_as_at}
\title{Statewide first-preference draws as at a date, leakage-guarded}
\usage{
statewide_draws_as_at(
  region,
  year,
  as_at,
  election_date,
  parties,
  n_sims = 20000L,
  party_cor = NULL,
  tpp_target = NULL,
  fallback_sd = 1.5,
  fp_extra_sd = 2.419,
  seed = NULL
)
}
\arguments{
\item{region}{Region code, e.g. \code{"vic"}, \code{"fed"}.}

\item{year}{Election year being predicted.}

\item{as_at}{Cutoff date. Polls after it are excluded. Pass election day
minus one day for a backtest.}

\item{election_date}{The election being predicted. Used ONLY by the leakage
assertion, which refuses a cutoff on or after it.}

\item{parties}{Character vector of party classes the seat model simulates.}

\item{n_sims}{Number of draws.}

\item{party_cor}{Optional correlation matrix over \code{parties}. Supply the same
one the published path uses; \code{NULL} draws independently.}

\item{tpp_target}{Optional \code{list(mean=, sd=)} two-party anchor. When given,
the Labor/Coalition split is moved to hit it, exactly as the published path
does. When \code{NULL} the trend's own two-party figure is used with the spread
implied by the trend band.}

\item{fallback_sd}{Per-party spread used when the trend gives no band for a
party. Not a modelling choice made here: it is the published path's own
fallback, kept identical so this measures what we ship.}

\item{fp_extra_sd}{First-preference variance correction, added in quadrature
to each party's trend band. \strong{This is not optional and not a tuning knob}:
\code{scripts/fit_seats_full.R} applies exactly this
(\code{sd_vec <- sqrt(trend_sd^2 + FP_EXTRA_SD^2)}, \code{FP_EXTRA_SD = 2.419},
adopted in \verb{docs/reviews/fp-widening-choice-*.md}), so omitting it here
would mean this function measures something the repo does not publish.

Omitting it is not hypothetical -- the first version of this function did,
understating statewide spread by roughly 2.6x, and the resulting backtest
came out MORE over-confident rather than less. That looked like a finding
about the seat model and was a missing constant.}

\item{seed}{Optional seed.}
}
\value{
A list with \code{draws} (\code{n_sims} x \code{parties} matrix, rows summing to
100), \code{folded} (parties the trend could not fit, folded into \code{OTH}),
\code{n_polls}, and \code{fp} (the trend's point estimate). \code{NULL} if the trend
cannot be fitted at that cutoff, which is a thin cycle rather than an error.
}
\description{
Builds the \code{statewide_draws} matrix \code{simulate_seat_contests()} expects, from
the poll trend as at \code{as_at} rather than from the election result. The
construction mirrors \code{scripts/fit_seats_full.R}: per-party spreads come from
the trend's own 95\% band, the draws are correlated across parties, and the
two-party total is anchored to a projection.
}
