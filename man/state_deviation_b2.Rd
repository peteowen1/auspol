% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_deviation.R
\name{state_deviation_b2}
\alias{state_deviation_b2}
\title{Two-predictor state-deviation coefficients (v2)}
\usage{
state_deviation_b2(
  cls,
  exclude_pair,
  oof = "output/xgb-primary-v6-oof-predictions.csv",
  dev = "output/state-deviation-features.csv",
  shuffle = 0L,
  lambdas = c(1, 3, 10, 30, 100, 300)
)
}
\arguments{
\item{cls}{\code{"ALP"} or \code{"LNP"}. Each gets its own coefficient rather than
assuming \code{b_LNP = -b_ALP}: the deviation is defined on the ALP swing, and
how much of it comes out of the Coalition rather than the minors is an
empirical question, not an identity.}

\item{exclude_pair}{The federal pair being predicted; excluded from the fit.}

\item{oof, dev}{Source tables.}

\item{shuffle}{Control seed. 0 is the real fit; any other integer permutes
which state each seat belongs to, within its election, destroying the
seat-to-state link while leaving every marginal and the whole procedure
intact. See \code{\link[=.sd_shuffle]{.sd_shuffle()}}.}

\item{lambdas}{Ridge penalty grid.}
}
\value{
Named numeric \code{c(b_poll, b_elec, lambda, n)}, or \code{NA}s when too
little to fit.
}
\description{
Arm of \verb{docs/plans/prereg-state-deviation-v2-2026-09-19.md}. Same
leave-target-out, one-row-per-state-year residual table as
\code{\link[=state_deviation_b]{state_deviation_b()}}, regressed on BOTH \code{state_poll_dev} and the decayed
prior state-election swing \code{state_elec_dev * exp(-gap / 24)}, by ridge
with the penalty chosen leave-one-election-out on the state-year table.
A state-year with neither source is dropped from the fit and gets no
correction at apply time.
}
