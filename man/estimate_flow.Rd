% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow_model.R
\name{estimate_flow}
\alias{estimate_flow}
\title{Estimate a party's preference flow for an election that has not happened}
\usage{
estimate_flow(
  flows,
  target_party,
  year,
  cycles = NULL,
  as_of = Sys.Date(),
  min_n = 5L
)
}
\arguments{
\item{flows}{From \code{\link[=load_preference_flows]{load_preference_flows()}}.}

\item{target_party}{Party code. Deliberately not named \code{party}: that is also
a column of \code{flows}, and data.table would bind the column instead.}

\item{year}{Election year to predict. Used only to report which record the
estimate came from; the estimator itself does not extrapolate to it.}

\item{cycles}{From \code{\link[=load_election_cycles]{load_election_cycles()}}; loaded if missing.}

\item{as_of}{Date defining which elections count as observed.}

\item{min_n}{How many recent estimates to average.}
}
\value{
List with \code{flow}, \code{se} (standard error of that mean), \code{n},
\code{n_avail}, \code{model}, \code{spread} and \code{years}; or \code{NULL} if nothing to use.
}
\description{
Averages the party's \code{min_n} most recent observed estimates, pooled across
regions. Chosen by strict temporal backtest over 102 elections, where it
beat a fitted trend, carry-forward and four other candidates -- see the
commentary at the top of this file. Pooling regions is deliberate:
preference behaviour moves on a national timescale and no single state has
enough elections to estimate it alone.
}
