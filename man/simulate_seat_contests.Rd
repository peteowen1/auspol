% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seat_sim.R
\name{simulate_seat_contests}
\alias{simulate_seat_contests}
\title{Simulate every seat from first preferences}
\usage{
simulate_seat_contests(
  shares,
  matrix,
  party_sd,
  seat_sd = 3.5,
  n_sims = 2000,
  smooth = 0.15,
  seed = NULL,
  statewide_draws = NULL
)
}
\arguments{
\item{shares}{Matrix or data.frame, one row per seat, one column per party,
holding each seat's CENTRAL projected first-preference share. Row names
(or a \code{seat} column) name the seats.}

\item{matrix}{From \code{\link[=build_flow_matrix]{build_flow_matrix()}}.}

\item{party_sd}{Named numeric: statewide uncertainty per party, in points.
Applied once per simulation and shared by every seat, so parties move
together across the state as they actually do. Ignored when
\code{statewide_draws} is supplied.}

\item{seat_sd}{Per-seat idiosyncratic deviation, in points. A single number
applies to every party; a named vector gives each party its own, which
matters when one party's seat share is ALLOCATED rather than measured. One
Nation polled 0.22\% in Victoria in 2022, so its seat shares are constructed
by ordering on Greens share and quantile-mapping onto South Australia --
an estimate with a measured RMSE of 5.0 points against SA's actual result,
against 3.5 for a party projected from its own prior seat vote. Giving both
the same figure claims the constructed number is as reliable as the
measured one. See docs/plans/prereg-onp-seat-uncertainty.md.}

\item{n_sims}{Number of simulations.}

\item{smooth}{Passed to the transfer step; see \code{\link[=distribute_preferences]{distribute_preferences()}}.}

\item{seed}{Optional RNG seed.}

\item{statewide_draws}{Optional \code{n_sims} x parties matrix of statewide
shares, one row per simulation. Supply this when the statewide
distribution is already known and calibrated — drawing each party
independently here and renormalising destroys the Labor-versus-Coalition
covariance, which measured at \strong{60\% of the projection's own two-party
spread} and made the seat range roughly 40\% too tight.}
}
\value{
List: \code{win_prob} (data.frame, one row per seat and party with a
probability), \code{totals} (matrix of seats won per party per simulation),
\code{fallback_rate} (share of transfers with no conditional cell).
}
\description{
Simulate every seat from first preferences
}
