% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/htv_flow.R
\name{htv_flow_override}
\alias{htv_flow_override}
\title{Apply the how-to-vote card to the Liberal-excluded, ALP+GRN-alive flow rows}
\usage{
htv_flow_override(ov, fm, target_election, seats, table = NULL, rows = NULL)
}
\arguments{
\item{ov}{Per-seat conditional list (from
\code{\link[=xgb_flow_conditional_override_for]{xgb_flow_conditional_override_for()}}) or \code{NULL}.}

\item{fm}{A \code{\link[=build_flow_matrix]{build_flow_matrix()}} result, used when \code{ov} is \code{NULL} or a
seat has no entry.}

\item{target_election}{Election label.}

\item{seats}{Seat names to cover.}

\item{table, rows}{Injectable for tests; default read from disk / fitted.}
}
\value{
The per-seat list (built from \code{fm$conditional} where needed),
with attribute \code{htv} = list(applied, share, order) -- or \code{ov} unchanged.
}
\description{
For every seat, every conditional key whose excluded party is LNP and
whose survivor set contains both ALP and GRN gets its ALP:GRN split
replaced by the fitted share for the card order recorded for that
election (and seat, where the table names one); every other survivor's
share is untouched. No table entry for the election: nothing changes.
}
