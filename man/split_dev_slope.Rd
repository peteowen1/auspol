% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_slope.R
\name{split_dev_slope}
\alias{split_dev_slope}
\title{Project a seat's class share with separate slopes for returning and departed vote}
\usage{
split_dev_slope(
  x,
  ret_frac,
  level_prev,
  level_now,
  s_ret,
  s_dep,
  fallback_frac = 1
)
}
\arguments{
\item{x}{Numeric vector of the class's seat-level shares at the PREVIOUS
election, in percentage points -- the same input \code{\link[=dev_slope]{dev_slope()}} takes.}

\item{ret_frac}{Numeric vector, same length as \code{x}: the share of that prior
vote held by candidates standing again, from
\code{\link[=returning_vote_fraction]{returning_vote_fraction()}}. \code{NA} falls back to \code{fallback_frac}.}

\item{level_prev, level_now}{Statewide class shares, as in \code{\link[=dev_slope]{dev_slope()}}.}

\item{s_ret, s_dep}{Slopes for the returning and departed portions.}

\item{fallback_frac}{Used where \code{ret_frac} is \code{NA} (no prior vote to
decompose). Defaults to 1, which reproduces the returning-candidate slope
-- chosen because a class with no prior vote has a deviation of
\code{-level_prev} either way and the two slopes differ least there.}
}
\value{
Numeric vector of projected shares, floored at zero.
}
\description{
The two-slope generalisation of \code{\link[=dev_slope]{dev_slope()}}. Where \code{dev_slope()} applies
one slope to the whole deviation \code{x - level_prev}, this splits \code{x} into the
portion held by candidates standing again and the portion held by those who
are not, allocates \code{level_prev} between them in the same proportion, and
gives each its own slope:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{projected = level_now + s_ret * (x*f - level_prev*f)
                      + s_dep * (x*(1-f) - level_prev*(1-f))
}\if{html}{\out{</div>}}

where \code{f} is \code{ret_frac}. \strong{At the extremes this reduces exactly to
\code{\link[=dev_slope]{dev_slope()}}}: \code{f = 1} gives \code{dev_slope(..., s_ret)} and \code{f = 0} gives
\code{dev_slope(..., s_dep)}. Only genuinely mixed classes move, which is the
population the change targets
(\verb{docs/plans/prereg-partial-return-split-slope-2026-09-09.md}).
}
