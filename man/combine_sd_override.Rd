% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reentry_prior.R
\name{combine_sd_override}
\alias{combine_sd_override}
\title{Combine two \code{sd_override} matrices without understating either}
\usage{
combine_sd_override(a, b)
}
\arguments{
\item{a, b}{Matrices of the same shape as \code{shares}, or \code{NULL}.}
}
\value{
The combined matrix, or \code{NULL} if both inputs are \code{NULL}.
}
\description{
A cell can be claimed by more than one uncertainty source -- a governed
salience candidate re-entering a seat their party did not contest last
time is both. \code{sd_override} accepts exactly one matrix, so the two must be
merged before the call. This takes the LARGER of the two wherever either
has an opinion (\code{NA} means "no opinion", the \code{sd_override} convention
throughout this package), rather than inventing a theory of how two
independent-but-unquantified uncertainty sources compose -- see the
refusal section of \verb{prereg-reentry-flatratio-variance-2026-09-08.md}.
}
\details{
Either argument may be \code{NULL}, in which case the other is returned
unchanged (and both \code{NULL} returns \code{NULL}), so a caller can pass this
straight through without an \code{if} at every call site.
}
