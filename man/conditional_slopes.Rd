% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dev_slope.R
\name{conditional_slopes}
\alias{conditional_slopes}
\title{Per-seat conditional slopes, from whether the same candidate stands again}
\usage{
conditional_slopes(
  cls,
  seats,
  returns,
  same = c(IND = 0.907, OTH_RIGHT = 0.891, GRN = 0.994, ONP = 0.61),
  new = c(IND = 0.326, OTH_RIGHT = 0.325, GRN = 0.88, ONP = 0.545),
  default = 1,
  same_mp = NULL
)
}
\arguments{
\item{cls}{The party class being projected.}

\item{seats}{Character vector of seat names, in the order the shares matrix
uses. The returned vector matches it element for element.}

\item{returns}{A \code{data.table} from \code{\link[=candidate_returns]{candidate_returns()}}, or \code{NULL} to leave
every seat on \code{default}.}

\item{same, new}{Named numeric vectors of slopes by class.}

\item{default}{Slope for a class absent from \code{same}/\code{new}.}

\item{same_mp}{Optional named numeric vector of slopes by class, applied
where the returning candidate was the SITTING MEMBER. \code{NULL} (the
default) keeps the two-tier \code{same}/\code{new} behaviour byte-identical.

\code{same} pools a returning member with a returning also-ran. Measured
separately over 531 returning non-major candidacies across 10 election
pairs, they are 0.954 (se 0.026) and 0.800 (se 0.046) -- about 2.9 SE
apart. Pooling shrinks an entrenched independent toward a ~5\% statewide
IND average every cycle, which is where most of this model's fed2025 seat
log-loss gap to AE Forecasts sits. Requires \code{returns} to carry a
\code{same_mp} column, which \code{\link[=candidate_returns]{candidate_returns()}} now supplies.}
}
\value{
Numeric vector the length of \code{seats}.
}
\description{
Builds the slope vector arm C needs: for each seat in \code{seats}, the
same-candidate slope where that class's candidate is returning and the
new-candidate slope where they are not.
}
\details{
Fitted across 17 election pairs. A single per-class slope averages two
populations that behave nothing alike, and is therefore wrong for every
individual seat:

\tabular{lrr}{
class \tab same \tab new \cr
IND \tab 0.907 \tab 0.326 \cr
OTH_RIGHT \tab 0.891 \tab 0.325 \cr
GRN \tab 0.994 \tab 0.880 \cr
ONP \tab 0.610 \tab 0.545
}

Classes with no entry in either table keep \code{default}, so a class the fit
never saw is left on uniform swing rather than given someone else's number.
}
