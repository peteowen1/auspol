% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{election_data_path}
\alias{election_data_path}
\title{Locate fetched election-commission data}
\usage{
election_data_path(subpath = "", must_exist = FALSE)
}
\arguments{
\item{subpath}{Path relative to \code{external/elections}.}

\item{must_exist}{Stop if absent. \code{FALSE} to test for an optional file.}
}
\value{
Absolute path.
}
\description{
Results fetched from electoral commissions (VEC, ECSA, AEC) live under
\verb{external/elections/}, alongside the anchor clone and gitignored for the
same reason: it is third-party data, none of these commissions publishes a
licence, and none of it is ours to redistribute. It is disposable — every
file is reproducible by rerunning the fetch scripts.
}
\details{
\verb{external/} rather than \verb{output/} deliberately. These are model INPUTS, so
clearing generated forecasts must not also delete an hour of fetching.

Naming is \verb{\{source\}-\{year\}-\{region\}-\{content\}.csv}, e.g.
\code{vec-2022-vic-transfers.csv}. Sortable, unambiguous, and extends to further
sources and elections without renaming what is already there.
}
