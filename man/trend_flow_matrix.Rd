% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow_trend.R
\name{trend_flow_matrix}
\alias{trend_flow_matrix}
\title{Extrapolate each flow cell along its own trend over prior elections}
\usage{
trend_flow_matrix(
  fm,
  tx,
  target_year,
  weight = 1,
  min_el = 3L,
  max_shift = 15,
  classes = NULL
)
}
\arguments{
\item{fm}{A flow matrix from \code{\link[=build_flow_matrix]{build_flow_matrix()}}, built on \code{tx}.}

\item{tx}{Transfer rows for PRIOR elections only. Every row must belong to an
election held before \code{target_year}; this is asserted, not assumed.}

\item{target_year}{Integer year being forecast.}

\item{weight}{Blend weight in \verb{[0, 1]} on the trend-extrapolated rate against
the flat estimate already in \code{fm}. 0 returns \code{fm} unchanged.}

\item{min_el}{Minimum distinct elections a cell needs before a trend is fitted
for it. Below this the cell keeps its flat value.}

\item{max_shift}{Cap, in percentage points, on how far one destination may be
moved. A trend fitted on three points can extrapolate absurdly; this bounds
the damage when it does.}

\item{classes}{Optional character vector of source classes to adjust. \code{NULL}
adjusts every class. Applying the trend to every class was measured FIRST
and is monotonically worse on fed2025 (0.3042 flat, 0.3040 at w = 0.25,
0.3045 at 0.50, 0.3065 at 1.00), so a restricted arm is only meaningful for
a class named in advance -- \code{docs/reviews/fed2025-closing-the-aef-gap}
already identified One Nation as the entire residual before this was
written, which is what makes \code{classes = "ONP"} a test of that claim rather
than a threshold chosen after seeing the answer.}
}
\value{
\code{fm} with \code{conditional}, \code{superset}, \code{pairwise} and \code{pooled} cells
adjusted and renormalised to sum to 100.
}
\description{
\code{build_flow_matrix()} averages every election it is given, so a preference
rate that is \emph{moving} is estimated at the middle of its own history and then
applied to an election at the end of it. That is a bias, not noise, and
widening the draw around it does not help -- measured: per-source flow
uncertainty at k = 0.25 and 0.50 scored 0.3043 and 0.3051 against a 0.3042
baseline, monotonically worse, because symmetric noise around a biased centre
only blurs the answer.
}
\details{
The bias is real and it is directional. Within the EXACT survivor set
\code{ALP+GRN+LNP}, One Nation's share to the Coalition ran 35.5, 45.4, 52.3,
49.7, 61.1 across federal elections -- and \code{OTH_RIGHT} to the Coalition ran
the other way, 49.2, 49.2, 42.5, 46.3, 43.3, 35.8, 20.4. Both are trends, in
opposite directions, which is why this is written as a general mechanism
rather than an One Nation adjustment.

Fitted on pre-2025 elections only, a weighted linear trend predicts 72.2 for
\code{ONP|ALP+LNP} in 2025 against an actual 71.4, where carrying the last value
forward gives 63.8.
}
\section{Why the cells and not the margin}{

The pooled marginal for a class is contaminated by which survivor sets
happened to occur. For One Nation, fed2022's rates re-weighted to fed2025's
mix move 47.6 -> 51.1, so about a third of the apparent 14-point swing is
composition and two thirds is real within-cell movement. Trending the
margin would fit the composition; trending the cells does not.
}

