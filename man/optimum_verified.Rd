% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyperpars.R
\name{optimum_verified}
\alias{optimum_verified}
\title{Is this point actually a local optimum, on the code's own evidence?}
\usage{
optimum_verified(par, value, fn, lower, upper, step = 0.05, tol = 1e-06)
}
\arguments{
\item{par, value}{The candidate point and its objective value.}

\item{fn}{The boxed objective.}

\item{lower, upper}{Box bounds.}

\item{step}{How far to probe in each coordinate.}

\item{tol}{How much better a neighbour must be to count as better.}
}
\value{
\code{TRUE} if the point survives all three checks.
}
\description{
Split out of \code{\link[=optim_boxed]{optim_boxed()}} so the REFUSAL half can be tested. Code 52 is
platform-dependent -- it appears on Linux CI and not on Windows -- so the
branch that consumes it cannot be driven from a developer machine at all,
and three separate attempts to provoke it locally produced only inputs that
L-BFGS-B certified unaided. A test of the whole branch would therefore have
passed without ever running the code it claimed to cover, which is this
repo's oldest hazard. As a named function the check can be handed
constructed inputs directly.
}
\details{
Three conditions, each answering a way Nelder-Mead's own \code{convergence == 0}
can be worthless:
\enumerate{
\item \strong{Inside the box.} \code{boxed()} returns a constant \code{1e12} outside it, and a
constant is perfectly flat and so perfectly "converged". Certifying there
would hand back a parameter outside its own bounds.
\item \strong{Not near the penalty.} Same reason, from the value side.
\item \strong{Nothing a short step away is better.} This is the one that means
optimum. Nelder-Mead stops on a RELATIVE tolerance, so where the
objective is numerically large -- a near-singular precision matrix gives
a huge but finite negative log-likelihood -- a simplex started at the
point under test satisfies it without having moved. Asking the objective
directly does not care how large it is.
}
}
\keyword{internal}
