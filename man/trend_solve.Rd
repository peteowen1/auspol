% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{trend_solve}
\alias{trend_solve}
\title{Posterior solve and exact log marginal likelihood}
\usage{
trend_solve(
  prep,
  sigma_obs,
  sigma_rw,
  sigma_house,
  anchor,
  firm_factors = NULL,
  want_var = TRUE
)
}
\arguments{
\item{want_var}{If FALSE, skip the posterior-variance solve (the expensive
part) — used inside the hyperparameter optimiser.}
}
\description{
Everything is Gaussian, so both the posterior and the evidence are exact:
log p(y) = 1/2 log|P| - 1/2 log|A| - n/2 log(2 pi) + 1/2 sum(log w)
- 1/2 ( sum(w y^2) + b0' P^-1 b0 - b' A^-1 b )
with A = P + H'WH, b = b0 + H'Wy. Two sparse Cholesky factorisations.
}
\keyword{internal}
