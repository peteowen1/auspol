% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgb_primary_override.R
\name{xgb_primary_predict_live}
\alias{xgb_primary_predict_live}
\title{Predict LIVE primary shares with the final XGBoost model (not a backtest)}
\usage{
xgb_primary_predict_live(
  shares,
  mat22,
  a22,
  state_mean,
  returns,
  own_prev = NULL,
  region = "vic",
  year = 2026L,
  prev_year = 2022L,
  enabled = NULL
)
}
\arguments{
\item{shares}{The just-computed shipped-model shares matrix (seats x
parties, summing to 100/row) -- becomes the \code{pred_share} feature.}

\item{mat22}{Seat-level 2022 class shares matrix, same shape as \code{shares}.}

\item{a22}{Named numeric vector, 2022 STATEWIDE class shares.}

\item{state_mean}{Named numeric vector, the forecast's statewide class
shares this cycle.}

\item{returns}{\code{candidate_returns()} output, or \code{NULL} before vic2026
nominations close (then same/same_mp default FALSE for every seat).}

\item{own_prev}{\code{personal_prior_vote()} output, or \code{NULL} before vic2026
nominations close (then \code{own_prev_pcv} is NA for every row, routed by
xgb's native missing-value handling, same as the backtest's treatment
of thin/absent cells).}

\item{region}{Single string; must be one of the six regions the final
model was trained on ("fed","nsw","qld","sa","vic","wa").}

\item{year}{Target election year, used for \code{load_seats(year, region)} and
for identifying this election's own rows in \code{output/candidacies.csv}
(\code{historic_elected}/\code{ballot_position}, both NA pre-nomination -- fine,
same missing-value routing).}

\item{prev_year}{Prior election year, for the corresponding prior-year
candidacy row counts.}

\item{enabled}{Logical; defaults to \code{AUSPOL_XGB_PRIMARY_LIVE} env var == "1".}
}
\value{
The overridden shares matrix, or \code{shares} unchanged if not
enabled or the saved model/cols files are missing.
}
\description{
Builds the same v6 feature columns \code{scripts/fit_xgb_primary_v6.R} builds
for the historical backtest, from the CURRENT forecast's own inputs, and
predicts with the model \code{scripts/fit_xgb_primary_v6_final.R} trained on
all 22 historical pairs (\code{output/xgb-primary-v6-final.model} /
\code{-v6-final-cols.json}). Same reference result as the backtest v6 arm:
pooled seat log loss 0.3403 -> ~0.3071 leave-one-pair-out, the best of
v1-v6 (\verb{docs/reviews/xgb-primary-v5-seat-features-2026-09-10.md} for v5;
v6 adds the salience/emergence features -- jump, governed, permit,
surge_h, is_recipient -- on top of v5's set, built specifically because
v5's live smoke test found a severe side effect: Victoria's statewide
IND/OTH_RIGHT predicted share collapsed ~10x/~180x across 87 seats, 55 of
87 predicting both classes exactly zero. v6 fixes this on the vic2022
backtest proxy (IND/OTH_RIGHT sums INCREASE slightly rather than
collapsing) -- but see the IMPORTANT CAVEAT below.
}
\details{
\strong{IMPORTANT CAVEAT, read before trusting this today}: the salience/surge
features (jump/governed/permit/surge_h/is_recipient) need a salience
corpus for the TARGET election. A PARTIAL one now exists for vic2026 --
\code{scripts/fetch_candidates_vic2026_prenomination.R} (Wikipedia, pre-
nomination, necessarily incomplete/unofficial) and
\code{scripts/fetch_seat_salience_vic2026_live.R} (real Google Trends data for
the IND/ONP/OTH_RIGHT candidates it found) feed into
\code{scripts/build_vic2026_salience_corpus.R}, which is what actually writes
into \code{output/salience-v6.csv} in the schema \code{governed_population()} reads
-- run that script (idempotent, safe to rerun) after either fetcher finds
new candidates. \strong{This is a real, separate step, not automatic} -- an
earlier claim that it "activates with no further code change" once
nominations close was wrong: the two fetch scripts and the file this
function reads were never connected until \code{build_vic2026_salience_corpus.R}
was built. Coverage today: 46 of 88 seats, IND/ONP/OTH_RIGHT only -- most
seats and all other classes still run on safe defaults (jump=0,
governed=0, permit=1 protect, surge_h=0, is_recipient=0). Re-run all three
scripts in sequence after 9 Nov nominations close and a fuller candidate
list exists, to extend coverage -- that part, and only that part, needs no
further code change.

\strong{Ship decision}: Pete's explicit, contemporaneous instruction this
session was to ship whichever variant has the best pooled seat log loss
and iterate on regressions afterward, rather than waiting for every named
case to clear. Known, named, NOT fixed by this: SA2026 One Nation remains
broken (log loss 0.4536, essentially unchanged from v5's 0.4537 -- adding
salience features did not help this specific case, since \code{is_recipient}
still never fires for SA2026 ONP, a separate, already-documented
surge-hazard misattribution bug) -- this is a deliberate, informed
tradeoff, not an oversight. \code{AUSPOL_XGB_PRIMARY_LIVE} in
\code{published_flags.R} is the switch; set it back to "0" to revert to the
shipped-only model with no other change needed.
}
