% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/salience_screen.R
\name{governed_population}
\alias{governed_population}
\title{The governed population for one election, built once and shared}
\usage{
governed_population(election, prev_election, region, surge_threshold = 5)
}
\arguments{
\item{election, prev_election}{Election labels as used in
\code{output/candidacies.csv} and \code{output/salience-v6.csv}.}

\item{region}{The region code (\code{"fed"}, \code{"vic"}, \code{"sa"}, \code{"nsw"}), for
\code{\link[=surging_parties]{surging_parties()}}.}

\item{surge_threshold}{Passed to \code{\link[=surging_parties]{surging_parties()}}.}
}
\value{
A \code{data.table} of \code{seat}, \code{party}, \code{keyword}, \code{jump}, \code{prev_party},
\code{elected}, \code{pcv}, \code{governed}, or \code{NULL} if \code{output/salience-v6.csv} has no
rows for \code{election}.
}
\description{
Wraps \code{\link[=candidate_returns]{candidate_returns()}} (by way of a per-candidate name match) and
\code{\link[=surging_parties]{surging_parties()}} into the single population lookup every consumer of
the salience screen needs: for each named candidate, is this someone the
screen (or any other salience-based mechanism) is entitled to make a claim
about? A sitting member returning under their own name, or a candidate of
a class whose statewide vote surged, is not -- see the \code{governed} parameter
of \code{\link[=salience_screen]{salience_screen()}}.
}
\details{
One implementation because five harnesses (and now more than one
salience-based mechanism) build this population five different ways, and a
governed-population definition assembled slightly differently in each is
how a bug like the Donato mismatch survives in one harness after being
fixed in another.
}
\section{Seat renames}{
 seat-name matching between elections only strips
case and punctuation, not genuine redistribution renames (Denison ->
Clark, Batman -> Cooper, Melbourne Ports -> Macnamara are corrected via a
small explicit lookup; others are not). An unmapped rename makes a
returning member look like a fresh governed candidate with \code{prev_party}
near zero -- known, disclosed gap, not silently assumed complete.
}

