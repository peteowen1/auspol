% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seat_share_rmse.R
\name{seat_share_rmse}
\alias{seat_share_rmse}
\title{Root-mean-square error of a seat-by-class vote-share point estimate}
\usage{
seat_share_rmse(shares, actual_votes)
}
\arguments{
\item{shares}{Numeric matrix, seats in rows (named) and party classes in
columns (named), percentages summing to ~100 per row.}

\item{actual_votes}{A data frame with \code{seat}, \code{party} and \code{votes} for the
election being predicted; aggregated here, so candidate-level rows are
fine.}
}
\value{
A list: \code{rmse} over every seat-class cell, \code{mae}, \code{by_class} (a
named vector of per-class RMSE), \code{n_seats} scored, \code{n_dropped}, and
\code{detail} -- a long \code{data.table} (\code{seat}, \code{party}, \code{pred_share},
\code{actual_share}) with one row per scored seat-class cell. \code{detail} exists
so a caller can persist the point estimate alongside the win probability
it already writes, rather than recomputing this function's \code{pred}/\code{act}
matrices a second time to get at them -- see
\verb{docs/reviews/rmse-persistence-2026-09-09.md}.
}
\description{
The second metric in this repo's objective, after seat log loss: how far
the projected first-preference share in each seat and class sits from the
share actually polled. Computed on the POINT estimate the simulator is
handed (\code{shares}), not on any draw, so it measures the swing model and the
candidate-level machinery without the Monte Carlo layer.
}
\details{
Seats are matched by name; a seat present in \code{shares} but absent from
\code{actual_votes} (a redistribution, a rename the harness did not map) is
excluded and counted in \code{n_dropped} rather than scored against zero, which
would inflate the error with a data-plumbing fault. A class present in one
and not the other is scored against zero, which is the true share.
}
