% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fundamentals.R
\name{build_fundamentals_data}
\alias{build_fundamentals_data}
\title{Assemble the fundamentals training table}
\usage{
build_fundamentals_data(
  parties = c("@TPP", "ALP", "LNP", "GRN", "ONP", "OTH"),
  min_year = 1990,
  polled_only = TRUE,
  require_actual = TRUE
)
}
\arguments{
\item{parties}{Party codes to include, plus "@TPP".}

\item{min_year}{Elections before this are dropped (the anchor uses 1990;
earlier Australian elections are structurally different and the minor
parties barely existed).}

\item{polled_only}{Restrict to elections with polling, so the same set can
be used for the projection stage.}

\item{require_actual}{Keep only elections whose result is known. Set FALSE
to build the feature row for an election that has not happened yet — the
whole point of a fundamentals model — in which case \code{actual} is \code{NA}.}
}
\value{
data.table with \code{actual} and the feature columns.
}
\description{
One row per (election, party). \verb{@TPP} is Labor's two-party share, so its
incumbency features are defined relative to Labor.
}
