% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_returns.R
\name{personal_prior_vote}
\alias{personal_prior_vote}
\title{A personally-returning leading candidate's OWN prior vote, under whatever
party they ran as then}
\usage{
personal_prior_vote(
  election_from,
  election_to,
  corpus = NULL,
  major_discount = NULL,
  pooled = NULL,
  loser_discount = NULL,
  minor_discount = NULL,
  minor_discount_loser = NULL
)
}
\arguments{
\item{election_from, election_to}{Election labels as they appear in the
corpus, e.g. \code{"fed2019"} and \code{"fed2022"}.}

\item{corpus}{Optional pre-read candidacy table; read from
\code{output/candidacies.csv} when \code{NULL}.}

\item{major_discount}{Optional numeric. When set, a candidate who was the
SITTING MEMBER for a major party and now stands for a non-major class
carries that fraction of their major-party vote as a FLOOR on the
class's seat base. \code{NULL} (the default) excludes prior major-party
registrations entirely, which is the previous behaviour.

Measured over 12 sitting members who did this, retention is 0.284 of
their major-party vote (sd 0.191). \code{major_discount} alone applies only to
sitting members; a losing (non-member) defector's own median retention is
0.142 over 12-13 corpus cases -- real and usable, not the "5 cases, mean
2.32, noise" this docstring claimed before 2026-09-09 (that mean was
distorted by one ratio taken on a ~2\% denominator). See \code{pooled} below
to extend the floor to them. It is a floor rather than a replacement
because the class may already have a
better base in that seat from a different candidate -- overwriting Kate
Hook's 20\% in Calare with Andrew Gee's discounted Nationals vote moved
the seat the wrong way, 0.122 -> 0.040.}

\item{pooled}{Extend the defector floor to major-party candidates who were
NOT the sitting member. \code{NULL} reads \code{AUSPOL_DEFECT_POOLED}. Before
2026-09-09 they were excluded outright and received nothing, on a
comment asserting their retention was unusable noise -- which was wrong:
13 cases with a median of 0.142, not 5 with an outlier-driven mean.}

\item{loser_discount}{Rate applied to those non-member defectors, when it
should differ from \code{major_discount}. \code{NULL} gives both groups the same
rate. Under \code{AUSPOL_DEFECT_POOLED="2"} this is resolved automatically
from \code{\link[=fit_defector_discount]{fit_defector_discount()}} rather than threaded through six
harnesses -- see docs/plans/prereg-defector-two-rate-2026-09-09.md.}

\item{minor_discount}{Optional numeric. When set, discounts \code{own_prev_pcv}
for a candidate who switched between two NON-major classes (e.g. ONP to
KAP/OTH_RIGHT) -- \code{prev_best} above matches on IDENTITY regardless of
party, so without this the old class's full, undiscounted result carries
forward as the new class's base. \code{NULL} (the default) leaves this
byte-identical to before the parameter existed. When \code{minor_discount_loser}
is also given (two-rate mode), this rate applies ONLY to a switcher whose
sitting-member status at the prior election is UNKNOWN (no \code{elected}
column) -- a confirmed sitting member gets NO discount at all (see
\code{minor_discount_loser}'s own doc for why), so this becomes the fallback
rate for the one case that is neither confirmed sitting nor confirmed
non-sitting. Without \code{minor_discount_loser}, this applies to every
switcher (the old, single-rate behaviour). The value the harnesses pass
is \code{\link[=fit_minor_defector_discount]{fit_minor_defector_discount()}}'s own pooled \code{discount} field -- a
MEDIAN over 18 corpus cases, 0.3255 for most targets. (The review's
headline "49\%" is a geometric mean at a lower \code{min_prior} and is NOT
what ships; see that function's docstring.) See
\verb{docs/reviews/minor-to-minor-defector-2026-09-16.md}.}

\item{minor_discount_loser}{Optional numeric. Rate applied to a
minor-to-minor (or minor-to-IND, IND-to-minor) switcher CONFIRMED NOT to
be the sitting member at the prior election. \code{NULL} (the default) gives
every switcher \code{minor_discount}'s single rate, byte-identical to before
this parameter existed.

A CONFIRMED SITTING MEMBER gets NO discount when this is set -- not a
fitted "sitting-member rate". Found 2026-09-17 tracing why Murray,
Orange and Barwon's real sitting-member Shooters-Fishers-and-Farmers-to-
Independent departures (retention 108-136\%) were badly under-predicted
by the single pooled rate, and revised 2026-09-18 after actually
shipping a fitted sitting-member rate and finding it made those same
three seats WORSE, not better: \code{\link[=fit_minor_defector_discount]{fit_minor_defector_discount()}}'s
leave-target-out fit for nsw2023 draws only on the other two sitting
cases (Mirani 0.79, Kennedy 0.63), giving 0.71 -- an underestimate for
seats that actually retained 108-136\%. Leave-one-out cross-validated
against all 5 sitting-member corpus cases (Barwon 1.36, Murray 1.30,
Orange 1.08, Mirani 0.79, Kennedy 0.63): predicting flat 1.0 (no
discount) gives HALF the squared error (0.405) of predicting each case
from the other four's median (0.782) -- n=5 is too thin to fit a rate
below 1 usefully, and the corpus median/mean (1.08 / 1.03) both sit
almost exactly on "keep it all" anyway. The 13-case non-sitting rate
(median 0.276) has no such problem and is used as fitted.

A switcher whose prior status is unknown (\code{prev_was_mp} is NA -- older
data with no \code{elected} column) gets \code{minor_discount}, not this rate --
"unknown" is never read as "definitely not the sitting member".
\verb{docs/reviews/minor-defector-two-rate-2026-09-17.md}.}
}
\value{
A \code{data.table} of \code{seat}, \code{party}, \code{own_prev_pcv} (\code{NA_real_} where
the leading candidate does not personally return).
}
\description{
\code{\link[=candidate_returns]{candidate_returns()}} and \code{\link[=screened_slopes]{screened_slopes()}} correctly identify a
returning candidate regardless of party -- Philip Donato held Orange with
49.1\% as a Shooter in 2019 and 53.1\% as an independent in 2023 -- and use
that to pick a gentler SLOPE. But the slope multiplies the seat's
CLASS-level prior vote (\code{mat[, "IND"]}), which is 0\% for Orange in 2019
since Donato was registered OTH_RIGHT then. A correct slope applied to a
near-zero base still projects him near zero: Dalton (Murray) and Butler
(Barwon) show the identical fault, all sitting members whose entire
personal incumbency vanished because their registered party changed
between elections. \code{candidate_returns()}'s own docs anticipated only half
of this -- "which label they stand under ... belongs to the party swing" --
and the party swing never picked it up, because it has no path from a
person's identity to their own history under a different label.
}
\details{
This closes that gap directly: for each seat/class where the LEADING
candidate personally returns (same matching as
\code{\link[=leading_candidate_returns]{leading_candidate_returns()}}) AND their prior registration was NOT a
major party, their own \code{pcv} from whichever (non-major) party they
contested under at the prior election -- to be used as \code{x} in
\code{\link[=dev_slope]{dev_slope()}} in place of the class's seat-level prior vote. The
major-party exclusion is deliberate, not an oversight -- see the inline
comment above \code{MAJ} for the Ward/McBride case that motivates it.
}
