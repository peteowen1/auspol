% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_statewide.R
\name{forecast_statewide_or_oracle}
\alias{forecast_statewide_or_oracle}
\title{Replace a harness's oracle statewide with the forecast when asked}
\usage{
forecast_statewide_or_oracle(
  region,
  year,
  election_date,
  parties,
  st_a,
  st_b,
  code = "BX0",
  n_sims = 20000L,
  seed = 42L,
  mode = Sys.getenv("AUSPOL_FORECAST_MODE", "0"),
  on_fail = c("stop", "skip")
)
}
\arguments{
\item{region, year, election_date, parties, st_a, n_sims, seed}{As
\code{\link[=forecast_statewide_for]{forecast_statewide_for()}}.}

\item{st_b}{Named numeric, the oracle statewide.}

\item{code}{The harness's log prefix (e.g. \code{"BV0"}).}

\item{mode}{\code{Sys.getenv("AUSPOL_FORECAST_MODE")} by default.}

\item{on_fail}{\code{"stop"} (default) or \code{"skip"}: what to do when no trend can be
fitted. \code{"skip"} prints a loud line and returns \code{NULL} so a multi-pair
harness can drop the pair; it never falls back to the oracle.}
}
\value{
Named numeric on \code{names(st_b)}, with attribute \code{oracle} (the
replaced values) when the forecast was used.
}
\description{
One block for all six harnesses (until 2026-09-19 fed and sa each carried
their own copy and nsw/qld/vic/wa had none: \code{docs/MODEL-REGISTRY.md}'s one
OPEN GAP). Under \code{AUSPOL_FORECAST_MODE=1} the statewide the seats swing
toward is \code{\link[=forecast_statewide_for]{forecast_statewide_for()}}'s poll-and-fundamentals projection as
at the day before; otherwise \code{st_b}, the target election's own counted
result, is returned unchanged (the oracle, kept only so a plain run stays
byte-identical while the switch is measured).
}
