% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_mode.R
\name{check_seat_totals}
\alias{check_seat_totals}
\title{Check seat totals against the per-seat probabilities that produced them}
\usage{
check_seat_totals(probs, totals, max_mean_gap = 0.5)
}
\arguments{
\item{probs}{Per-seat win probabilities for one party.}

\item{totals}{That party's simulated seat total, one entry per simulation.}

\item{max_mean_gap}{Tolerated difference between the mean total and the sum
of probabilities. Monte Carlo noise on the mean is \code{sd/sqrt(n_sims)}, many
times smaller than this at any usable \code{n_sims}, so a larger gap is a bug.}
}
\value{
A list with \code{mean_total}, \code{expected}, \code{mean_gap}, \code{sd_total},
\code{floor_sd}, \code{sd_ratio} and \code{ok}.
}
\description{
Two identities tie a set of per-seat win probabilities to the distribution of
the seat total, and any simulation must satisfy both whatever model produced
it:
}
\details{
\enumerate{
\item \strong{The expected total is the sum of the probabilities}, exactly. The total
is a sum of Bernoulli indicators and expectation is linear, however
strongly the seats correlate.
\item \strong{The variance is at least the independent-seat variance}, \verb{sum p(1-p)}.
Seats sharing a statewide draw are positively correlated, and positive
correlation can only add variance. A total tighter than that floor is
arithmetically impossible.
}

This replaced a cross-check against the retired two-party seat model, which
\code{CLAUDE.md} forbids keeping. The reference here is arithmetic rather than
another model, which makes it both rule-compliant and stronger: it cannot
agree with a wrong answer because the other model shares the error.
}
