% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reentry_prior.R
\name{reentry_apply_harness}
\alias{reentry_apply_harness}
\title{One-call re-entry prior for a harness}
\usage{
reentry_apply_harness(mat, fa, fb, state_share, target, pairs, code = "RE1")
}
\arguments{
\item{mat}{Seat-by-class share matrix.}

\item{fa, fb}{Long-form \code{seat}, \code{party}, \code{votes} for the PREVIOUS and TARGET
elections.}

\item{state_share}{Named projected statewide share per class.}

\item{target}{Target election label, removed from the fit.}

\item{pairs}{Every candidate pair for the fit.}

\item{code}{Log prefix, e.g. \code{"BV1r"}.}
}
\value{
\code{mat}, with attribute \code{"reentry"} when applied.
}
\description{
Wraps \code{\link[=reentry_fit]{reentry_fit()}}, \code{\link[=seat_lean]{seat_lean()}} and \code{\link[=apply_reentry_prior]{apply_reentry_prior()}} so a harness
needs one line rather than six, and so all six harnesses do the same thing.
Returns \code{mat} unchanged, with a message, if the switch is off or anything
fails — a harness must never be silently skipped, which is the failure this
repo keeps recording.
}
