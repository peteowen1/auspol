% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/education_residual.R
\name{education_residual_b}
\alias{education_residual_b}
\title{Correct a primary prediction toward what education explains about its error}
\usage{
education_residual_b(
  cls,
  exclude_pair,
  feature = "yr12_pct",
  oof = "output/xgb-primary-v6-oof-predictions.csv",
  census = "output/census-features.csv",
  shuffle = 0L
)
}
\arguments{
\item{cls}{Party class, one of the pre-registered \code{ONP}, \code{OTH_RIGHT}, \code{GRN}.}

\item{exclude_pair}{The pair being predicted; excluded from the fit.}

\item{feature}{Census column to use. The pre-registered placebo is
\code{"born_aus_pct"}, which matched education exactly in the reallocation
test -- if it matches again the mechanism is "correct toward anything
correlated", not education.}

\item{oof, census}{Source tables.}
}
\value{
Single numeric coefficient, or \code{NA_real_} if the class has too few
training rows.
}
\description{
Fits ONE coefficient per class on the residual, LEAVE-ONE-PAIR-OUT, and
applies it as an offset. The existing prediction is left intact -- this is
not the wholesale reallocation refused in
\verb{docs/plans/prereg-class-concentration-v2-2026-09-15.md}, which threw the
per-seat predictions away and doubled pooled RMSE.
}
\details{
Tested against \verb{docs/plans/prereg-education-residual-correction-2026-09-15.md}.

\code{z} is standardised WITHIN pair. Mean Year 12 completion drifts 51.4 to 60.8
across the corpus, so a raw value carries that drift between elections --
the fault \verb{reviews/xgb-primary-sd-and-census-2026-09-12.md} found when
census was tried as a model feature.

LEAKAGE: the coefficient for pair X is fitted on every pair EXCEPT X, from
out-of-fold predictions. Nothing from the election being predicted reaches
its own coefficient.
}
