% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection.R
\name{fit_projection_mix}
\alias{fit_projection_mix}
\title{Optimal trend-versus-fundamentals weight at each horizon}
\usage{
fit_projection_mix(dat, w_grid = seq(0, 1, by = 0.01))
}
\arguments{
\item{dat}{From \code{\link[=build_projection_data]{build_projection_data()}}, with a \code{fund_tpp} column of
leave-one-out fundamentals predictions.}

\item{w_grid}{Candidate weights.}
}
\value{
data.table, one row per horizon: \code{horizon}, \code{n}, \code{w}, \code{mae_mix}
(in-sample, optimistic), \code{mae_mix_loo} (held out), \code{mae_trend},
\code{mae_fund}, \code{bias}, \code{sd_err} (in-sample) and \code{sd_err_loo} (held out —
the one the published intervals use).
}
\description{
For each horizon, finds the weight \code{w} minimising mean absolute error of
\code{w * trend + (1 - w) * fundamentals}, and measures the spread of the
resulting errors so a band can be attached.
}
\details{
The fundamentals prediction MUST be the leave-one-out one, or the mix is
fitted against a fundamentals number that has already seen the election it
is being scored on, which biases the weight toward fundamentals.
}
