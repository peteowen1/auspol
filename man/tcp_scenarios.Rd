% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tcp_scenarios.R
\name{tcp_scenarios}
\alias{tcp_scenarios}
\title{Per-seat final-two scenario frequencies from a simulate_seat_contests() run}
\usage{
tcp_scenarios(sim)
}
\arguments{
\item{sim}{The list returned by \code{\link[=simulate_seat_contests]{simulate_seat_contests()}}. Must have been
run with the default TCP tracking (always on; there is no flag to turn
it off).}
}
\value{
A \code{data.table} with one row per (seat, scenario), where a
"scenario" is an unordered party pair, sorted so \code{"ALP+LNP"} and
\code{"LNP+ALP"} are the same row. Columns: \code{seat}, \code{f1}, \code{f2} (\code{f1} is
whichever party won the MAJORITY of that pairing's own draws, so it
reads as "the pairing's own favourite", not an artefact of party
order), \code{freq} (share of ALL draws for that seat landing on this
pairing -- frequencies for one seat sum to 1, uncontested seats
excepted), \code{f1_tcp_pct} (median \link{f1}'s two-candidate-preferred share
across just the draws in this pairing). Ordered by seat then
descending \code{freq}, so \verb{[seat == "X"][1]} is always that seat's
most-likely pairing -- the same "headline scenario" AEF's own site
shows and \code{scripts/build_aef_tcp.R} extracts from their data.
}
\description{
Per-seat final-two scenario frequencies from a simulate_seat_contests() run
}
