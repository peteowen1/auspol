% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold.R
\name{refold_unfitted}
\alias{refold_unfitted}
\title{Fold an unfitted party back into "Others"}
\usage{
refold_unfitted(polls, fits, parties = NULL, oth = "OTH")
}
\arguments{
\item{polls}{A cycle's polls from \code{\link[=cycle_polls]{cycle_polls()}}.}

\item{fits}{Named list from \code{\link[=fit_cycle_trends]{fit_cycle_trends()}}; used only to determine
which parties are fitted.}

\item{parties}{Parties to consider; default every polled party that is
neither a major nor the residual category.}

\item{oth}{Name of the residual column.}
}
\value{
A copy of \code{polls} with \code{oth} raised on the affected rows, carrying
attribute \code{refolded}: what was added where.
}
\description{
When a party is polled but NOT fitted -- it fell under a script's inclusion
floor -- \code{oth} means two different things within one cycle: firms that break
the party out report \code{oth} excluding it, firms that fold it in report \code{oth}
including it. The model fits a single column across both and cannot see the
difference; on NSW 2023 it read a ~5-point definitional gap as a Morgan house
effect of -0.28, and fitted \code{oth} at 15.30 against an actual of 17.96.
}
\details{
\code{\link[=unfold_others]{unfold_others()}} handles the opposite case and cannot help here: imputing a
party out of \code{oth} needs a fitted trend, and an unfitted party has none.
This goes the other way and needs no model at all -- the party's share is
reported on exactly the rows being corrected.

Only parties absent from \code{fits} are touched. A fitted party is
\code{\link[=unfold_others]{unfold_others()}}'s business, and doing both to one party would undo itself.
}
