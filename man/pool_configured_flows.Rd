% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external_flows.R
\name{pool_configured_flows}
\alias{pool_configured_flows}
\title{Pool whichever external flows the environment has switched on}
\usage{
pool_configured_flows(tx, before, quiet = FALSE)
}
\arguments{
\item{tx}{\code{data.table} of transfers for the election being predicted from.}

\item{before}{Date, or a string \code{as.Date()} accepts, of the election being
PREDICTED. Not the election \code{tx} came from.}

\item{quiet}{Suppress the progress line. The line reports which elections
were admitted; \code{CLAUDE.md} records an experiment whose edit never ran and
whose byte-identical output read as "this input does not matter", so the
default is to print what was applied.}
}
\value{
\code{tx} with any switched-on, date-admissible transfers appended.
}
\description{
The harness-facing wrapper. Four backtest scripts each carried their own
copy of this gating, and a fifth jurisdiction would have doubled that to
eight; one of the four had drifted into defining a gate it never called
while still labelling its output as though it had.
}
\details{
Both sources default OFF. \code{AUSPOL_QLD_CUTOFF} and \code{AUSPOL_WA_CUTOFF} cap
what each may admit, independently, for the control described in
\code{pool_external_flows()}. They are deliberately per-source: one shared cutoff
held every source out at once, so a control meant to isolate one of them
silently removed the other.
}
