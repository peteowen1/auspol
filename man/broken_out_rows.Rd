% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fold.R
\name{broken_out_rows}
\alias{broken_out_rows}
\title{Rows where a party is reported separately and "Others" excludes it}
\usage{
broken_out_rows(polls, party, sum_range = c(97, 103), oth = "OTH")
}
\arguments{
\item{polls}{A cycle's polls from \code{\link[=cycle_polls]{cycle_polls()}}.}

\item{party}{The party that may be reported separately.}

\item{sum_range}{Reported-FP total that counts as "already complete".}

\item{oth}{Name of the residual column.}
}
\value{
Logical vector, one per poll row.
}
\description{
The mirror of \code{\link[=folded_rows]{folded_rows()}}. That one finds rows where a party is missing
because its votes sit inside \code{oth}; this finds rows where the party IS
reported and \code{oth} therefore does not contain it.
}
\details{
Identified the same way, arithmetically: a row whose reported first
preferences already sum to ~100 \strong{with} \code{party} present must have an \code{oth}
that excludes it. A row that only reaches ~100 once \code{party} is removed is
already folded and is left alone.
}
