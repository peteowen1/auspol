% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/candidate_returns.R
\name{remove_transferred_votes}
\alias{remove_transferred_votes}
\title{Take a transferred personal vote OUT of the class it came from}
\usage{
remove_transferred_votes(mat, own_prev)
}
\arguments{
\item{mat}{Numeric matrix, seats in rows (named) and party classes in
columns (named): the prior-election seat base the simulator starts from.}

\item{own_prev}{The table from \code{\link[=personal_prior_vote]{personal_prior_vote()}}, or \code{NULL} (returns
\code{mat} unchanged).}
}
\value{
\code{mat} with the transfers removed. Seats or classes absent from
\code{mat} are skipped, not an error: a class the target election does not
field has no column to adjust.
}
\description{
Companion to \code{\link[=personal_prior_vote]{personal_prior_vote()}}. That function reports, per seat and
class, the leading candidate's own prior vote under whatever label they
stood under last time (\code{own_prev_pcv}), and which class that was
(\code{prev_party}) with the amount moved (\code{transfer}). Callers substitute
\code{own_prev_pcv} into the new class's seat base; this function does the
other half and subtracts \code{transfer} from \code{prev_party}'s seat base in the
same seat, floored at zero. Without it one vote is counted twice.
}
