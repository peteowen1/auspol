% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dev_slope.R
\name{screened_slopes}
\alias{screened_slopes}
\title{Per-seat slopes conditioned on candidate identity AND the salience screen}
\usage{
screened_slopes(
  cls,
  seats,
  returns,
  permit,
  same = c(IND = 0.907, OTH_RIGHT = 0.891, GRN = 0.994, ONP = 0.61),
  new = c(IND = 0.326, OTH_RIGHT = 0.325, GRN = 0.88, ONP = 0.545),
  default = 1,
  same_mp = NULL
)
}
\arguments{
\item{cls}{The party class being projected.}

\item{seats}{Character vector of seat names, in the order the shares matrix
uses. The returned vector matches it element for element.}

\item{returns}{A \code{data.table} from \code{\link[=candidate_returns]{candidate_returns()}}, or \code{NULL} to leave
every seat on \code{default}.}

\item{permit}{Logical vector the length of \code{seats}, from
\code{\link[=salience_screen]{salience_screen()}}: does the screen allow this seat's candidate of \code{cls}
to emerge?}

\item{same, new}{Named numeric vectors of slopes by class.}

\item{default}{Slope for a class absent from \code{same}/\code{new}.}

\item{same_mp}{Passed through to \code{\link[=conditional_slopes]{conditional_slopes()}}; see there.}
}
\value{
Numeric vector the length of \code{seats}.
}
\description{
Arm C (\code{conditional_slopes()}) split a class into two: a returning candidate
(slope 0.907) and a new one (slope ~0.33). Measured across five harnesses it
was refused -- the new-candidate slope is fitted on ~300 candidates who are
overwhelmingly no-hopers, so it crushed the rare emergent toward the mean and
hurt every emergence election it touched: vic2018 +0.191 log loss, fed2022
+0.143, sa2026 +0.114.
}
\details{
Salience separates exactly that rare group. \code{salience_screen()} refuses a
governed candidate who never registers -- 709 of them across five elections,
zero winners -- and permits one who does, or one the screen makes no claim
about. This adds a THIRD slope for that permitted-but-new group: uniform swing
(1.0), because there is no fitted value for "new candidate who fires" and
shrinking them is precisely the failure being fixed.

\tabular{lll}{
group \tab condition \tab slope \cr
returning \tab same person stood here before \tab 0.907 etc, per class \cr
screened out \tab new, governed, screen refuses \tab ~0.33, per class \cr
screen-permitted \tab new, and either ungoverned or fired \tab 1.0 (uniform)
}
}
