% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poll_tracking.R
\name{poll_tracking_check}
\alias{poll_tracking_check}
\title{Compare each fitted endpoint against the polls behind it}
\usage{
poll_tracking_check(
  cp,
  fits,
  window = 90L,
  min_polls = 3L,
  bound = POLL_TRACKING_BOUND
)
}
\arguments{
\item{cp}{Cycle polls, as returned by \code{\link[=cycle_polls]{cycle_polls()}}.}

\item{fits}{Named list of fits, as returned by \code{\link[=fit_cycle_trends]{fit_cycle_trends()}} or
\code{\link[=fit_cycle_unfolded]{fit_cycle_unfolded()}}.}

\item{window}{Days before the last poll to average over.}

\item{min_polls}{Parties with fewer polls in the window are reported but not
asserted on — there is nothing to be close to.}

\item{bound}{Maximum permitted |fitted − mean poll|.}
}
\value{
data.table: \code{party}, \code{fitted}, \code{poll_mean}, \code{n}, \code{dev}, \code{asserted},
\code{breach}. Attribute \code{bound} carries the threshold used.
}
\description{
Compare each fitted endpoint against the polls behind it
}
